---
title: "TEMPO: Semantic-Action Decoupled RL Post-Training for Vision-Language-Action Models"
title_zh: TEMPO：面向视觉-语言-动作模型的语义-动作解耦强化学习后训练
authors: "Ziheng Liu, Quantao Yang"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07314v1"
tags: ["query:robot-rl"]
score: 9.0
evidence: 对VLA策略的强化学习后训练
tldr: VLA模型从预训练到下游操作通常用SFT或RL后训练，但SFT存在分布不匹配，而现有RL对所有组件统一更新，忽视功能差异。TEMPO冻结视觉语言骨干，将语义投影层与低层动作专家解耦，分别采用低频与高频更新，既保持语义稳定又快速融入交互反馈。在CALVIN和真实任务上，TEMPO一致超越预训练SOTA与RL基线，并稳定获得更高奖励。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.07314v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 867, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07314v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1810, \"height\": 948, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07314v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1843, \"height\": 827, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.07314v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 906, \"height\": 656, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07314v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 180, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07314v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 911, \"height\": 182, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07314v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 905, \"height\": 238, \"label\": \"Table\"}]"
motivation: 现有后训练统一更新策略忽略VLA组件功能差异，SFT又存在分布不匹配，导致语义表征易受干扰。
method: 冻结预训练视觉语言骨干，将语义投影层与动作专家解耦，以低频和高频双时间尺度分别更新，兼顾语义稳定与反馈学习。
result: 在CALVIN基准与真实操作任务上，TEMPO一致超越预训练SOTA和RL后训练基线，且真实任务奖励更高更稳定。
conclusion: 解耦双时间尺度RL后训练有效平衡语义保持与动作学习，为VLA模型策略优化提供了新范式。
---

## 摘要
视觉-语言-动作（VLA）模型通常通过监督微调（SFT）或在线强化学习（RL）后训练来适应下游操作任务。SFT容易产生分布不匹配，而现有的RL方法通常对所有模型组件采用单一、统一的更新策略，忽略了它们不同的功能角色。我们提出了TEMPO，一种面向VLA模型的语义-动作解耦、双时间尺度RL后训练框架。TEMPO冻结预训练的视觉-语言主干以保留通用语义表示，并将适应性调整限制为两个具有专用RL优化循环的组件：语义投影层和低级动作专家。我们以不同的速率更新它们——语义投影层不频繁，以保持潜在动作稳定；动作专家频繁，以快速整合来自在线交互的控制反馈。这种解耦的RL微调策略防止快速策略更新破坏高层语义表示的稳定性，同时仍允许动作专家从在线反馈中高效学习。在CALVIN基准和真实世界操作任务上的实验表明，TEMPO持续优于预训练的最先进VLA模型和RL后训练基线，同时在两个真实世界任务上达到并保持更高的评估奖励。

## Abstract
Vision-language-action (VLA) models are commonly adapted to downstream manipulation tasks via supervised fine-tuning (SFT) or online reinforcement learning (RL) post-training. SFT is prone to distribution mismatch, and existing RL approaches typically apply a single, uniform update strategy to all model components, ignoring their distinct functional roles. We propose TEMPO, a semantic-action decoupled, two-timescale RL post-training framework for VLA models. TEMPO freezes the pretrained vision-language backbone to preserve general semantic representations, and restricts adaptation to two components with dedicated RL optimization loops: the semantic projection layer and the low-level action expert. We update them at different rates--the semantic projection layer infrequently, to keep the latent action stable, and the action expert frequently, to rapidly incorporate control feedback from online interaction. This decoupling RL fine-tuning strategy prevents fast policy updates from destabilizing high-level semantic representations while still allowing the action expert to learn efficiently from online feedback. Experiments on the CALVIN benchmark and real-world manipulation tasks demonstrate that TEMPO consistently outperforms both pretrained state-of-the-art VLA models and the RL post-training baseline, while reaching and maintaining higher evaluation rewards on two real-world tasks.

---

## 论文详细总结（自动生成）

# TEMPO：面向视觉-语言-动作模型的语义-动作解耦强化学习后训练 —— 中文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- 视觉-语言-动作（VLA）模型在预训练后，通常需要通过**监督微调（SFT）**或**在线强化学习（RL）后训练**来适配下游机器人操作任务。
- 现有方法存在两个关键问题：
  - **SFT 的局限**：容易产生**分布不匹配**（distribution mismatch），即训练数据分布与在线交互/真实部署分布不一致，导致策略泛化能力受限。
  - **现有 RL 后训练的局限**：通常对所有模型组件采用**单一、统一的更新策略**，忽略了 VLA 内部各组件的**功能差异**——例如视觉语言骨干负责高层语义表征，而动作解码器负责低频/高频控制。
- 因此，本文旨在解决“如何在 RL 后训练中同时保持预训练语义表征的稳定性，又能快速学习动作反馈”这一核心矛盾。

## 2. 论文提出的方法论

- **核心思想**：将 VLA 模型的 RL 后训练分解为**语义**与**动作**两个解耦的优化过程，采用**双时间尺度（two-timescale）**更新机制。
- **具体技术细节**：
  - **冻结预训练视觉-语言骨干**：保留通用语义表示，防止策略更新破坏高层特征。
  - **将适应性调整限制为两个专用组件**：
    1. **语义投影层（semantic projection layer）**：负责将语义表示映射到潜在动作空间。
    2. **低级动作专家（low-level action expert）**：负责生成具体控制动作。
  - 为这两个组件分别设置**独立的 RL 优化循环**，并以不同频率更新：
    - 语义投影层：**低频更新（infrequent）**，以保持潜在动作稳定。
    - 动作专家：**高频更新（frequent）**，以快速整合在线交互的控制反馈。
- **算法流程（文字描述）**：
  1. 预训练 VLA 模型后，冻结视觉语言骨干。
  2. 初始化语义投影层和动作专家，并为二者建立独立 RL 优化器与更新调度。
  3. 与真实/仿真环境交互，收集轨迹与奖励。
  4. 对语义投影层和动作专家分别执行不同频率的策略梯度更新（文中未给出具体公式）。
  5. 循环直至收敛。
- **核心优势**：解耦+双时间尺度可防止快速策略更新破坏高层语义表示，同时允许动作专家高效地从在线反馈中学习。

## 3. 实验设计

- **使用的数据集/场景**：
  - **CALVIN 基准**：一种常见的机器人操作仿真 benchmark，用于评估连续指令跟随与操作能力。
  - **真实世界操作任务**：论文提到“两个真实世界任务”（two real-world tasks），但未在给定文本中详细说明具体任务名称、环境或物体。
- **对比方法**：
  - **预训练的最先进 VLA 模型**（pretrained SOTA VLA models）。
  - **RL 后训练基线**（RL post-training baseline），即统一更新所有组件的 RL 方法。
- **评估指标**：评估奖励（evaluation reward），以及任务成功率等相关指标（文中主要强调奖励）。

## 4. 资源与算力

- 论文给定文本中**未明确说明**使用的 GPU 型号、数量、训练时长、显存消耗或其他算力资源。
- 因此，无法从现有内容中总结算力细节。

## 5. 实验数量与充分性

- 从给定文本看，实验包括：
  - CALVIN benchmark 上的评估。
  - 两个真实世界操作任务上的评估。
  - 对比预训练 SOTA 与 RL 后训练基线。
- **充分性评价**：
  - 优点：覆盖了仿真与真实场景，对比了直接 SFT/预训练与统一 RL 后训练，能说明通用场景下的有效性。
  - 不足：给定文本中**没有提供具体实验组数、超参数设置、消融实验（如不同更新频率比例、是否冻结骨干等）的细节**，也没有给出表格数据。因此实验数量在文本层面看似有限，不足以全面验证各组件的贡献及双时间尺度的最优比例。
  - 客观性：摘要声称 TEMPO 持续优于两种基线，但缺少具体数值与统计显著性信息，需结合全文进一步评估。

## 6. 主要结论与发现

- TEMPO 在 **CALVIN 基准**和**真实世界操作任务**上，一致优于预训练 SOTA VLA 模型和 RL 后训练基线。
- 在两个真实任务上，TEMPO 能够达到并保持更高的评估奖励。
- 验证了“语义-动作解耦 + 双时间尺度 RL 后训练”能够有效平衡**语义保持**与**动作学习**之间的矛盾。

## 7. 优点

- **问题洞察清晰**：准确指出现有 RL 后训练忽略组件功能差异的缺陷，并针对性地提出解耦策略。
- **方法设计简洁有效**：冻结骨干、低频更新语义投影层、高频更新动作专家，逻辑清晰且易于实现。
- **兼顾稳定性与学习效率**：双时间尺度更新机制在理论上避免了灾难性遗忘与语义漂移，同时保证动作专家能快速适应在线反馈。
- **实验场景覆盖较广**：同时包含仿真基准（CALVIN）和真实机器人操作任务，验证了方法的实际可行性。
- **后训练范式创新**：为 VLA 模型的策略优化提供了一种新的“分而治之”思路。

## 8. 不足与局限

- **算力与资源细节缺失**：未报告训练/推理所用 GPU 等硬件信息，不利于复现与成本评估。
- **实验细节不充分**：给定文本未提供各任务的奖励曲线、成功率具体数值、基线的超参数配置、以及消融实验（如更新频率比例、是否冻结骨干）等。
- **真实任务覆盖有限**：仅提到两个真实世界任务，任务类型和复杂度未知，对通用性的证明有限。
- **可迁移性存疑**：方法依赖“语义投影层+动作专家”的架构假设，对结构不同的 VLA 模型（如无独立动作专家）是否适用未讨论。
- **理论保证缺失**：双时间尺度的收敛性、稳定性等性质未从理论层面进行证明。
- **潜在偏差风险**：摘要由作者自述，可能存在选择性报道有利结果的风险；需查看全文确认实验公平性。

（完）
