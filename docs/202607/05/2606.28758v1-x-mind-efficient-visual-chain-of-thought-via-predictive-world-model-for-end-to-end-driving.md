---
title: "X-Mind: Efficient Visual Chain-of-Thought via Predictive World Model for End-to-End Driving"
title_zh: "X-Mind: 通过预测世界模型实现高效视觉思维链的端到端驾驶"
authors: "Bohao Zhao, Chengrui Wei, Guangfeng Jiang, Ruixin Liu, Xuejie Lv, Liu Liang, Sutao Deng, Xiuyang Fan, Pengkun Zheng, Jinyun Zhou, Rui Guo, Hanpeng Liu, Yutong Zheng, Yi Guo, Xinlong Zheng, Qingyu Luo, Zhuangzhuang Ding, Yu Zhang, Hang Zhang, Xianming Liu"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28758v1"
tags: ["query:vla-policy"]
score: 9.0
evidence: 提出将预测世界模型作为视觉思维链集成到VLA模型中用于驾驶
tldr: 当前视觉-语言-动作模型缺乏预测未来状态的能力，为此提出X-Mind框架，将预测世界模型内化为视觉链式思考。模型在动作前先推理未来世界状态，采用紧凑BEV草图及深度压缩自编码器将12帧未来演变压缩至96个token，并引入循环块扩散机制将去噪迭代折叠进骨干网络一次前向。在大规模真实数据上取得竞争力的端到端驾驶性能，实现了资源受限平台上的快速认知推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28758v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 593, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1158, \"height\": 651, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1407, \"height\": 580, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 882, \"height\": 969, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1452, \"height\": 940, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28758v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1215, \"height\": 217, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28758v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1309, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28758v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1214, \"height\": 218, \"label\": \"Table\"}]"
motivation: 现有VLA模型缺乏预测能力，而集成预测世界模型存在级联延迟或作为浅层任务无法深入嵌入前向推理的问题。
method: 提出X-Mind，将预测世界模型内化为视觉链式思考；用BEV草图融合驾驶先验作为紧凑思维表示，DC-AE压缩至96tokens；引入循环块扩散加速生成。
result: 在大规模真实驾驶数据集上达到竞争力十足的端到端驾驶性能，验证了低延迟实用性。
conclusion: X-Mind成功将大规模认知推理部署到资源受限车辆平台，提供高效低延迟的自动驾驶解决方案。
---

## 摘要
预测未来状态对于自主智能体至关重要，然而当前的视觉-语言-动作（VLA）模型本质上缺乏这种能力，依赖于反应性的感知-动作映射。虽然集成预测世界模型（PWM）可以弥补这一缺陷，但现有方法要么引入难以承受的级联延迟，要么仅作为浅层的终端任务，未能深入嵌入前瞻性推理。为了赋予VLA模型这种推理能力，我们提出了X-Mind。该框架并非将PWM视为外部辅助模块，而是将其内化为视觉思维链（Visual CoT）。通过在动作执行前强制执行世界推演，模型被迫首先想象未来的演化，从而产生一种稳健地基于环境动力学并意识到其行动将产生的未来后果的驾驶策略。这里的挑战在于效率，我们从两个方面解决。首先，我们引入了一种紧凑的视觉思维表示：一种融合鸟瞰图（BEV）布局与抽象驾驶先验（如导航意图和交通规则）的抽象草图。模型不是推演密集的未来帧，而是将这张草图作为思维画布进行推理；借助深度压缩自编码器（DC-AE），12帧的未来推演仅减少到96个token，缓解了长上下文计算瓶颈。其次，为了进一步加速生成，我们提出了一种循环块扩散方案，该方案将去噪步骤展开到大型驾驶模型的各层中，将迭代细化折叠到主干网络的一次前向传播中。在大规模真实世界数据上进行的训练和验证表明，X-Mind实现了具有竞争力的端到端驾驶性能，使其成为一种高度实用、低延迟的解决方案，成功地将大规模认知推理直接部署到资源受限的车辆平台上。

## Abstract
Predicting future states is essential for autonomous agents, yet current Vision-Language-Action (VLA) models fundamentally lack this capability, relying instead on reactive perception-action mapping. While integrating Predictive World Models (PWMs) addresses this gap, existing approaches either incur prohibitive cascaded latency or act as shallow terminal tasks that fail to deeply embed forward-looking reasoning. To endow VLA models with this reasoning capability, we propose X-Mind. Rather than treating PWMs as an external auxiliary module, this framework internalizes them as the Visual Chain-of-Thought (Visual CoT). By enforcing a world rollout prior to action, the model is constrained to imagine future evolution first, yielding a driving policy that is robustly grounded in environmental dynamics and aware of the future consequences its actions will unfold. The challenge here is efficiency, and we tackle it on two fronts. First, we introduce a compact representation of visual thinking: an abstract sketch that fuses a Bird's-Eye-View (BEV) layout with abstract driving priors (e.g., navigation intents and traffic rules). Rather than rolling out dense future frames, the model reasons over this sketch as a mental canvas; aided by a Deep Compression Autoencoder (DC-AE), a 12-frame future rollout is reduced to merely 96 tokens, alleviating the long-context computational bottleneck. Second, to accelerate generation further, we propose a recurrent block diffusion scheme that unrolls the denoising steps across the layers of the large drive model, folding iterative refinement into the backbone's one forward pass. Trained and validated on large-scale real-world data, X-Mind achieves competitive end-to-end driving performance, which makes it a highly practical, low-latency solution that successfully deploys large-scale cognitive reasoning directly onto resource-constrained vehicle platforms.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前的视觉-语言-动作（VLA）模型虽然推动了端到端自动驾驶的发展，但其本质上是**反应式**的——直接从感知映射到动作，缺乏显式的**未来预测**能力。而真正的物理智能体必须能预测未来（因果关系沿时间轴展开）以做出正确决策。
- **背景与动机**：将预测世界模型（PWM）集成到VLA模型是共识，但现有方案存在两大缺陷：
  - 级联范式将预测与规划分离，导致延迟过高；
  - 终端附加辅助任务方式使得梯度难以回传到深层大模型骨干，模型仍容易陷入捷径学习。
- **整体含义**：X-Mind旨在将PWM真正**内化**为视觉思维链（Visual CoT），强制模型在动作前先“想象”未来世界演化，从而获得基于物理动力学的稳健驾驶策略，同时解决车载部署的效率瓶颈。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将预测世界模型（PWM）作为视觉思维链嵌入大型语言模型（LLM）骨干中，而不是级联或终端附加。模型通过一个“心理画布”（abstract sketch）进行高效未来推理，并利用循环块扩散（Recurrent Block Diffusion, RBD）将迭代去噪折叠进单次前向传播。

- **关键技术细节**：
  1. **视觉思维表示（Visual Thinking Representation）**  
     - 采用**抽象草图**（abstract sketch）替代密集的未来帧或3D重建。该草图融合：
       - **BEV物理场景**：道路拓扑、动态智能体、本车等；
       - **抽象驾驶先验**：交通灯状态（左上信号面板）、自适应导航路径（青色走廊）、速度合规条（左下速度条，绿-白-红分别表示实际速度、剩余裕度、超速）。
     - 使用**深度压缩自编码器（DC-AE）** 将12帧草图压缩为**仅96个token**，解决长上下文瓶颈。
  2. **循环块扩散（Recurrent Block Diffusion, RBD）**  
     - 基于**层流匹配（Layer Flow Matching, LFM）** 思想：将标准的扩散/流匹配过程**内化**到LLM的各层中。
     - **训练阶段**：设置非均匀时间步长 {0, 0.1, 0.2, 0.4, 0.7, 1.0} 对应5个注入层。在每个指定层，用噪声与目标潜变量的线性插值替代草图token的隐藏状态；然后在提取层预测速度场 `v_k`（轻量Transformer编码器共享参数）。
     - **推理阶段**：从纯噪声 `z_0` 开始，通过欧拉积分在各层逐步去噪：`z_{k+1} = z_k + (t_{k+1} - t_k) * v_k`，最终解码得到预测的未来草图序列。
     - 实现**单次前向传播**完成未来生成，几乎不增加额外延迟。

- **整体流程**：输入多视图图像、文本指令、本车状态 → 编码为token → 通过RBD在LLM各层逐步生成未来草图 → 解码后的草图条件输入逆动力学规划头 → 输出纵向加速度和横摆角速度控制的轨迹。

### 3. 实验设计：使用的数据集/场景、基准、对比方法
- **数据集**：大规模内部自动驾驶数据集，约**280,000小时**连续驾驶记录，分割为**3400万**视频片段。传感器为7个环视摄像头（前鱼眼、前窄、左前、右前、左后、右后、后视）。大约86.8%城市路况、13.2%高速路况。视觉token总量13.8T tokens。本文实验使用该数据集**八分之一子集**。
- **基准（Benchmark）**：主要评估指标：
  - **ADE（平均位移误差）**：横向（Lat.）和纵向（Lon.）在6秒预测时的ADE。
  - **FID（Fréchet Inception Distance）**：评价未来预测的视觉质量。
  - **推理延迟**（相对于Base模型的倍数）。
- **对比方法**：
  - 标准VLA基线（Base：无未来推理）；
  - Base + 图像未来帧（3584额外token）；
  - Base + 3DGS（3072额外token）；
  - Base + 草图（单步去噪 vs. RBD）；
  - 不同预测目标（当前帧、未来1帧、未来12帧）。

### 4. 资源与算力
- 论文**未明确提及**具体的GPU型号、数量、训练时长等算力资源。仅提到训练数据使用某大型内部数据集中的一个八分之一子集。因此无法给出具体算力统计。

### 5. 实验数量与充分性
- **主要实验**：共呈现**3个量化表格**（Tab.1-3）和**多处定性可视化**（Fig.4-6）：
  - Tab.1：不同场景表示（图像、3DGS、草图）对比，验证草图在token效率和性能上的优势。
  - Tab.2：扩散架构对比（Base、Base+单步去噪、Base+RBD），体现RBD显著降低FID并提升规划精度。
  - Tab.3：预测目标对比（当前帧、未来1帧、未来12帧），说明未来12帧预测对规划最有利，尽管FID略高。
  - 定性结果：可视化不同驾驶场景（安全制动、变道、交通灯响应、拓扑避障）下的轨迹对比。
  - 此外还有结构化草图标注分析（Fig.5）和定性预测对比（Fig.4）。
- **充分性评估**：实验设计较为全面：
  - 验证了表示选择、扩散机制、预测长度三个关键维度；
  - 消融控制了骨干和训练配置相同；
  - 指标涵盖规划性能（ADE）、生成质量（FID）、效率（推理延迟）。但**缺少与其他SOTA端到端方法**（如DriveVLM、UniAD等）的直接对比表格；仅与自身基线对比。可能受限于使用内部数据集无法直接对比公开基准（如nuScenes）。总体来说实验较充分，但公平性受限于数据集私有。

### 6. 论文的主要结论与发现
- **核心结论**：X-Mind成功地将预测世界模型作为视觉思维链内化到VLA模型，实现了高效、低延迟的未来推理与规划。
- **关键发现**：
  - 抽象草图表示在极低token开销（96 tokens）下比图像或3DGS更优，兼顾效率与规划性能。
  - RBD机制比单步去噪大幅提升预测质量（FID从67.30降到9.59），且几乎不增加延迟。
  - 未来12帧预测比当前帧重构或单帧预测更能提供规划相关的监督，尽管生成更难（FID略高）。
  - 模型能自动推断遮挡或缺失的动态物体运动，说明学到了一定因果动力学。

### 7. 优点：方法或实验设计上的亮点
- **方法亮点**：
  - **深度内化**：将PWM作为Visual CoT嵌入LLM骨干，而非外部模块，避免了梯度回传困难和额外推理延迟。
  - **紧凑表示设计**：首次提出融合BEV物理布局和抽象驾驶先验的草图，大幅降低token开销（96 tokens），同时保持关键语义。
  - **循环块扩散**：创新地利用LLM分层结构实现单次前向传播的渐进去噪，实现超快生成。
  - **自监督潜力**：未来可向无监督学习扩展（文中提及）。
- **实验亮点**：
  - 系统消融了表示、扩散架构、预测长度等关键设计，结论清晰；
  - 定性可视化丰富，展示了模型在多种复杂场景下的合理预测与轨迹。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **数据集局限**：使用内部私有数据集，无法与公开基准（如nuScenes、Waymo）直接比较，限制了可复现性和普适性。且实验仅用了八分之一子集，规模虽大但仍可能引入数据偏差。
- **缺乏与SOTA方法直接对比**：论文仅对比自身基线，未与DriveVLM、UniAD等端到端方法在相同指标下比较，说服力打折扣。
- **表示依赖人工先验**：抽象草图中的驾驶先验（导航路径、速度条等）需要人工定义和标注，增加工程成本，且可能难以覆盖所有边缘场景（如不规则交通标志）。
- **规划头简单**：逆动力学规划仅输出加速度和横摆角速度，缺乏对多模态轨迹、不确定性量化的考虑。
- **实时性验证不充分**：虽然声称延迟低（1.1倍于Base），但未给出具体毫秒数，也未在真实车载平台上测试延迟和资源占用。
- **长期预测退化**：12帧预测FID更高，说明长时预测质量下降，文中未讨论如何确保长时间推理的可靠性。
- **缺少公开代码和模型**：不利于社区复现和进一步研究。

（完）
