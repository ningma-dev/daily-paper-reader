---
title: Preference-Calibrated Human-in-the-Loop Reinforcement Learning for Robotic Manipulation
title_zh: 偏好校准的人在回路强化学习用于机器人操作
authors: "Zeyi Liu, Guangyao Liu, Yinuo Qu, Yuquan Xue, Bofang Jia, Chunhua Yang, Weihua Gui, Keke Huang, Ziwei Wang"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03949v1"
tags: ["query:robot-rl"]
score: 9.0
evidence: 偏好校准的人类在环强化学习用于机器人操纵
tldr: "在机器人操作中，人机交互强化学习通过在线人类干预提升样本效率，但成功轨迹中常包含次优动作。现有方法对所有转换一致分配折扣终端奖励，导致Q值高估。本文提出PACT框架，利用干预隐含偏好信号对次优片段进行信用重分配。通过进步模型识别次优片段，构建偏好对定义反事实优势来校准Bellman目标，并在约束均值空间直接对齐策略与人类纠正动作。在五个真实机器人操作任务中，PACT将平均成功率提升24.5%，收敛速度加快1.3倍。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03949v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1345, \"height\": 396, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03949v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03949v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1346, \"height\": 849, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03949v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1356, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03949v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1407, \"height\": 415, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03949v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1306, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03949v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1018, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03949v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 610, \"height\": 240, \"label\": \"Table\"}]"
motivation: 现有HIL-RL方法对成功轨迹中所有转换统一分配折扣奖励，忽略次优动作的实际贡献，导致Q值高估和策略偏向次优行为。
method: 提出PACT框架，包含进步模型识别次优片段，利用干预信号构建偏好对计算反事实优势校准Q值，并在有界均值空间直接对齐策略与人类动作。
result: "在五个真实机器人操作任务中，平均成功率提升24.5%，收敛速度加快1.3倍。"
conclusion: PACT有效利用干预偏好信号修正信用分配，提升样本效率与操作成功率。
---

## 摘要
人在回路强化学习（HIL-RL）通过在线人类干预提高了真实机器人操作中的样本效率。然而，成功轨迹可能包含偏离期望任务执行路径并迫使人类干预的次优动作。现有的HIL-RL方法通常对所有迁移应用一致的信用分配原则，通过次优段均匀传播折扣终端奖励，忽略了每个迁移对任务成功的实际贡献。这高估了评论家学习的Q值，并间接引导演员更新朝向次优行为模式。为此，我们提出PACT，一种偏好校准的演员-评论家训练框架，利用干预隐含的偏好信号对识别的次优段进行信用重新分配，同时直接引导策略训练以实现无偏的评论家-演员学习。具体而言，我们首先设计一个从人类演示中学习并识别次优段以进行信用校正的进展模型。然后，从干预状态下的人类动作和重采样的策略动作中，我们构建偏好对以定义一个反事实优势，惩罚所识别次优段的贝尔曼目标，实现定向信用校准。此外，我们直接在受限均值空间中用人类纠正动作对齐策略，为评论家引导的更新提供额外信号。在五个真实机器人操作任务中，PACT将平均成功率提高了24.5%，并实现了1.3倍的更快收敛，从而提高了强化学习的样本效率和性能。代码可在https://anonymous.4open.science/r/HILRL-A1X-BC05获取。

## Abstract
Human-in-the-loop reinforcement learning (HIL-RL) improves sample efficiency in real-robot manipulation through online human intervention. However, successful trajectories may include suboptimal actions that deviate from the desired task-execution path and force human intervention. Existing HIL-RL methods typically apply the consistent credit assignment principle to all transitions, uniformly propagating discounted terminal rewards through suboptimal segments, ignoring the actual contribution of each transition to task success. This overestimates Q-values for critic learning and indirectly misguides actor updates toward suboptimal behavior patterns. To this end, we propose PACT, a Preference-calibrated Actor-Critic Training framework that leverages the implicit preference signals induced by intervention to perform credit reassignment on identified suboptimal segments while directly guiding policy training for unbiased critic-actor learning. Specifically, we first design a progress model that learns from human demonstration and identifies suboptimal segments for credit correction. Then, from the human action and resampled policy action at the intervention state, we build preference pairs to define a counterfactual advantage that penalizes Bellman targets of the identified suboptimal segment, enabling directional credit calibration. Moreover, we directly align the policy with human corrective actions in the bounded mean space, providing an additional signal beyond critic-guided updates. Across five real-robot manipulation tasks, PACT improves the average success rate by 24.5% and achieves 1.3 times faster convergence, thereby improving both RL sample efficiency and performance. Code is available at https://anonymous.4open.science/r/HILRL-A1X-BC05.

---

## 论文详细总结（自动生成）

# 论文《Preference-Calibrated Human-in-the-Loop Reinforcement Learning for Robotic Manipulation》详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- **研究背景**：人在回路强化学习（HIL-RL）通过引入实时人类干预，显著提升了真实机器人操作中的样本效率和安全性。但现有方法（如HIL-SERL）将所有过渡（transition）视为同质样本，采用一致的信用分配原则——在稀疏终端奖励下，将最终成功信号均匀传播至整个轨迹。
- **核心问题**：含有干预的成功轨迹中，正常探索、次优动作段与人类恢复段是异质的。次优动作（如错误抓取、路径偏移）本应对成功贡献甚微，却被统一赋予高折扣累积奖励，导致Q值高估，并间接引导策略（actor）学习次优行为模式，降低收敛速度。
- **整体含义**：需要一种细粒度的信用重分配机制，利用人类干预隐含的偏好信号（在干预点人类纠正动作优于策略动作），校正次优片段的估值，并直接引导策略优化。

## 2. 方法论：核心思想、关键技术细节、算法流程

### 核心思想
提出 **PACT**（Preference-calibrated Actor-Critic Training）框架，将干预轨迹视为异构结构化数据，通过三阶段校准实现无偏的评论家-演员学习：
- ① **次优段定位**：轻量级进度模型从演示中自监督学习，识别干预前的异常进度下降段。
- ② **评论家校正**：构建偏好对（人类纠正动作 vs. 策略动作），计算反事实优势，位置加权后惩罚该段的Bellman目标。
- ③ **演员辅助优化**：在受限均值空间（tanh-squashed mean）直接对齐策略与人类纠正动作，提供超出评论家引导的额外信号。

### 关键技术细节

#### 1. 进度模型定位次优段（Progress Model）
- **输入表示**：手腕/侧视图像残差（相对初始帧）+ 机器人自身位移 → 编码为特征 `o_t`。
- **自监督训练**：用均匀进度 `p_t^* = t/T` 作为伪标签 + 终端段的折扣蒙特卡洛回报损失。
- **定位规则**：当预测进度在短窗口内下降超过阈值 `δ_reg`，标记起点 `t_a`；当出现连续K步上升，标记恢复点 `t_r`；区间 `[t_a, t_r]` 为候选次优段。

#### 2. 偏好感知反事实校正（Preference-Aware Counterfactual Correction）
- **反事实优势**：在干预状态 `s_{th}` 处，计算 `A_cf = max( Q_bar(s_th, a^π) - Q_bar(s_th, a^human), 0 )`，并停止梯度。
- **位置权重**：`ω(t) = exp( -β · (t_r - t)/(t_r - t_a) )`，使靠近干预后半段的校正更强。
- **校正Bellman目标**：对次优段内每个时间步 `t`，`y_corr = y_std - ω(t)·A_cf`；其余段保持不变。
- **评论家损失**：最小化与校正目标之间的TD误差（双Q网络平均）。

#### 3. 偏好辅助策略优化（Preference Auxiliary Policy Optimization）
- **偏好对**：由干预状态 `s_th` 处人类动作和策略重采样动作构成。
- **目标函数**：`L_pref = E[ ||tanh(μ(s)) - a_human||^2 + 1/(||tanh(μ(s)) - a_policy||^2 + ε) ]`，仅通过squashed mean反向传播，避免对数概率的数值不稳定。
- **联合演员损失**：`L_actor = L_RLPD + λ_pref · L_pref`。

### 算法流程（文字说明）
1. 收集演示数据 `D_demo`，训练进度模型 `f_ψ`。
2. 在线RL过程中，人类可随时干预并记录干预状态和动作。将采集到的轨迹存入回放缓冲 `B`，并将干预点偏好对存入偏好缓冲 `B_pref`。
3. 每次训练时，从 `B` 中采样批次；对成功干预轨迹，用进度模型识别次优段 `[t_a, t_r]`。
4. 评论家：对次优段内样本使用校正Bellman目标；其他样本使用标准Bellman目标；计算均方误差损失。
5. 演员：结合RLPD损失和偏好损失（从 `B_pref` 采样）更新策略。
6. 迭代直至收敛。

## 3. 实验设计

### 数据集/场景
- **真实机器人任务**：5个难度递增的操作任务（Press、Insertion、Pick、Pick & Place、Assembly），在Galaxea A1X 6-DOF机械臂上执行。
- **演示数据**：每个任务采集20条演示轨迹（`D_demo`），用于进度模型训练和RL早期稳定。

### 基准与对比方法
- **主要基线**：**HIL-SERL**（SOTA HIL-RL框架），将人类干预轨迹纳入回放，使用熵正则化RLPD目标。
- **评估指标**：最终成功率（SR）、平均人类干预率（IR）、训练时间（分钟）。

### 消融实验
- 在Insertion任务上，移除评论家校正模块（C1）、移除演员偏好损失（C2），与完整PACT对比：完整模型成功率90%，干预率32.5%；去除C1后成功率60%，去除C2后干预率45.0%。

## 4. 资源与算力

- **文中未明确说明**具体的GPU型号、数量、训练时长分布。仅提到“训练时间”以分钟计（约21-122分钟不等），但未提及计算硬件配置。因此无法准确评估算力需求。

## 5. 实验数量与充分性

- **实验数量**：在5个真实任务上对比PACT与HIL-SERL，每个任务报告最终指标且给出完整训练曲线（成功率、干预率随步数变化）。额外进行了：
  - 进度模型定性可视化（3个任务的轨迹）。
  - 进度模型定量评估（两个运动学指标：路径效率η、方向偏差Δθ，对比正常段与次优段）。
  - Q值校正分析（训练阶段Q值演化、单轨迹价值剖面、归一化偏差累积，以及5个任务的蒙特卡洛偏差表）。
  - 消融实验（2个变体对比）。
- **充分性评价**：
  - **正面**：覆盖多个任务，对比基线为SOTA，消融实验验证各组件贡献，Q值分析提供直接证据。
  - **不足**：
    - 所有任务使用相同机器人，未在其他平台或仿真中验证泛化性。
    - 缺乏与更多替代方法（如带显式奖励塑形、逆强化学习等）的对比。
    - 消融仅在单一任务上进行，未在其他任务重复验证。
    - 统计显著性未报告（如多次运行的标准差，仅有“平均”值）。

## 6. 主要结论与发现

- PACT在5个真实任务上平均提升成功率24.5%（从58.0%至82.5%），降低干预率14.8%（从47.1%至32.3%），加快1.3倍收敛。
- 进度模型定位的次优段具有显著的路径效率低、方向偏差大特点，验证了识别有效性。
- PACT有效抑制了传统HIL-RL方法在次优段上的Q值高估（表3显示正偏变为负偏）。
- 演员侧偏好损失主要降低干预率，评论家校正影响成功率，两者互补。

## 7. 优点

- **创新性**：首次将干预轨迹的异构性显式建模，利用零成本偏好信号进行段级信用重分配，概念简洁且有效。
- **轻量实用**：进度模型仅需演示数据自监督训练，无需额外标注；整体框架可插拔（plug-and-play）地集成到现有HIL-RL流程（如SERL）。
- **实验扎实**：在真实机器人上完整运行，不仅有最终指标，还包含过程曲线、Q值分析、消融实验和运动学评价，多角度论证方法论的有效性。
- **代码开源**：提供匿名代码链接，促进可复现性。

## 8. 不足与局限

- **进度模型依赖**：基于演示的自监督可能受非单调轨迹或重复执行模式影响，定位误差会传播到评论家校正，导致不准确甚至有害修正。
- **校正粒度**：反事实优势提供的是方向性校正而非精确的每步价值目标，适合抑制系统高估但不适合精细校准。
- **实验覆盖**：
  - 仅在单一种类机械臂（Galaxea A1X）上验证，未评估跨平台、多物体、长时序任务的迁移能力。
  - 缺乏与基于大语言模型或视觉语言模型的进步推理方案对比。
  - 消融实验仅限单任务，结论泛化性存疑。
- **计算资源未报告**：无法判断方法在低算力设备上的可行性。
- **安全性讨论**：未讨论干预策略失败或错误干预（如人类判断失误）对训练的影响。

（完）
