---
title: "Q-VGM: Q-Guided Value-Gradient Matching for Flow-Matching VLA Policies"
title_zh: Q-VGM：面向流匹配VLA策略的Q引导值梯度匹配
authors: "Ziqian Wang, Jiayu Sun, Xingjian Mao, Minqian Wang, Yao Mu"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08015v1"
tags: ["query:robot-rl"]
score: 9.0
evidence: 通过值梯度匹配对流匹配VLA策略进行RL微调
tldr: "针对流匹配VLA策略微调中，直接反向传播值函数到去噪链存在数值不稳定的问题，本文提出Q-VGM方法。它利用VGG-Flow将值梯度转化为去噪时间上的值梯度场，避免通过去噪链反传。结合Cal-QL集成的行动敏感评论家，在固定回放缓冲上训练。在LIBERO等基准上，Q-VGM将成功率从75.0%提升至92.5%，在真实机器人任务上也显著优于基线。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08015v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1508, \"height\": 508}, {\"url\": \"assets/figures/arxiv/2606.08015v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1504, \"height\": 852}, {\"url\": \"assets/figures/arxiv/2606.08015v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 556}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08015v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1457, \"height\": 437}, {\"url\": \"assets/tables/arxiv/2606.08015v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1121, \"height\": 459}, {\"url\": \"assets/tables/arxiv/2606.08015v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1078, \"height\": 510}, {\"url\": \"assets/tables/arxiv/2606.08015v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 921, \"height\": 224}, {\"url\": \"assets/tables/arxiv/2606.08015v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 750, \"height\": 457}]"
motivation: 现有值基方法难以高效微调流匹配VLA策略，因直接反向传播值函数不稳定且策略梯度需要可处理的动作似然。
method: 提出Q-VGM，通过VGG-Flow将值梯度转化为去噪时间上的梯度场，避免反传去噪链；使用Cal-QL集成评论家并注入每层动作。
result: "在LIBERO上成功率从75.0%提到92.5%；RoboTwin 2.0上从76.4%提到87.2%；真实机器人任务从40.0%提到67.5%。"
conclusion: Q-VGM实现了高效的流匹配VLA策略微调，无需专家监督，显著提升任务性能，且在多个基准上优于同骨干和同评论家的基线。
---

## 摘要
我们提出Q引导值梯度匹配（Q-VGM），一种离策略强化学习（RL）方法，解决了微调流匹配视觉-语言-动作（VLA）策略中一个长期存在的挑战：高效地改进一个具有表达力的流匹配动作专家相对于学习到的Q函数。有效的改进必须利用评论家的一阶（梯度）信息，但对于流策略来说这很困难，因为通过其多步去噪过程直接反向传播值函数在VLA规模下数值不稳定，而策略梯度方法所需的可处理动作似然在迭代去噪下不可用。现有的基于值的方法要么通过整个去噪链反向传播，仅在测试时使用评论家而不更新策略，要么将评论家改进的动作作为终端标签蒸馏而不监督速度场。Q-VGM通过利用VGG-Flow（生成建模中流对齐的值梯度视图）规避了这些问题，将值梯度转化为去噪时间的值梯度场，而非不稳定的端到端目标。这不需要动作似然，也不需要通过去噪链反向传播，并在固定重放缓冲区上运行。评论家是一个基于紧凑RLT特征（每层动作注入）的动作敏感的Cal-QL集成。Q-VGM实现了一种实用的少样本初始化然后从经验中学习的范式：从少样本SFT pi0.5 VLA开始，该方法利用自生成的 rollout 数据显著提升任务性能，无需额外的专家监督。在LIBERO上，Q-VGM将平均成功率从75.0%提升至92.5%；在RoboTwin 2.0上，从76.4%提升至87.2%；在两个真实机器人桌面任务上，从40.0%提升至67.5%，在所有三种设置下均优于所有相同骨干网络、相同评论家的基线。

## Abstract
We propose Q-Guided Value-Gradient Matching (Q-VGM), an off-policy reinforcement learning (RL) method that tackles a long-standing challenge in fine-tuning flow-matching vision-language-action (VLA) policies: efficiently improving an expressive flow-matching action expert with respect to a learned Q-function. Effective improvement must exploit the first-order (gradient) information of the critic, but this is difficult for flow policies, because directly back-propagating the value through their multi-step denoising process is numerically unstable at VLA scale, while the tractable action likelihoods required by policy-gradient methods are unavailable under iterative denoising. Existing value-based methods either backpropagate through the full denoising chain, use the critic only at test time without updating the policy, or distill critic-improved actions as terminal labels without supervising the velocity field. Q-VGM sidesteps these issues by leveraging VGG-Flow, a value-gradient view of flow alignment in generative modeling that transforms value gradient into a denoising-time value-gradient field rather than an unstable end-to-end objective. This requires no action likelihoods and no backpropagation through the denoising chain, and operates on a fixed replay buffer. The critic is an action-sensitive Cal-QL ensemble over compact RLT features with per-layer action injection. Q-VGM enables a practical few-shot initialization then learn-from-experience paradigm: starting from a few-shot-SFT pi0.5 VLA, the method leverages self-generated rollout data to substantially improve task performance without additional expert supervision. On LIBERO, Q-VGM raises the average success rate from 75.0% to 92.5%; on RoboTwin 2.0, from 76.4% to 87.2%; and on two real-robot tabletop tasks, from 40.0% to 67.5%, outperforming all same-backbone, same-critic baselines across all three settings.

---

## 论文详细总结（自动生成）

# 论文总结：Q-VGM：面向流匹配VLA策略的Q引导值梯度匹配

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：视觉-语言-动作（VLA）模型已成为通用机器人控制的主流范式，其中流匹配（flow-matching）策略因其能够捕捉多模态动作分布而备受青睐。然而，在从模仿学习（如少样本SFT）微调到强化学习（RL）时，现有方法面临困境：策略梯度方法需要可处理的动作似然，但流策略通过迭代去噪生成动作，无法直接提供；而基于值的方法（如Diffusion-QL）直接反向传播值函数通过整个去噪链，在VLA规模（十亿参数）下数值不稳定。
- **核心挑战**：如何在不通过去噪链反向传播、不依赖动作似然的前提下，利用评论家（critic）的一阶梯度信息（∇_A Q）有效改进流匹配VLA策略。
- **整体意义**：提出一种离策略（off-policy）值引导微调方法，使得少样本SFT的VLA策略能从自身生成的rollout数据中自主学习、显著提升任务成功率，无需额外的专家示范或在线交互。这项工作衔接了生成式流建模与强化学习的值函数优化，提供了实用且稳定的微调方案。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：借鉴VGG-Flow中的最优控制框架，将策略改进视为去噪过程中的随机最优控制问题，最优速度修正正比于去噪时间值函数的梯度。Q-VGM通过评论家Q(s, A)的梯度构造速度修正，并通过残差速度匹配（residual velocity matching）训练动作专家，避免直接反向传播去噪链。
- **关键技术细节**：
  1. **动作敏感评论家**：在固定rollout缓冲区上训练Cal-QL集成的评论家。采用RL Token（RLT）从冻结的VLM前缀中提取紧凑状态特征（z_rl ∈ R²⁰⁴⁸），每层注入扁平化的动作块（per-layer action injection），确保Q对动作变化敏感。
  2. **前瞻清洁动作估计**：对每个去噪状态x_k，利用冻结的基础策略（base velocity）通过一步欧拉前推得到清洁动作估计Â_base。
  3. **迭代Q梯度上升与keep-best选择**：从Â_base开始执行J步投影梯度上升（梯度裁剪、动作范围裁剪），然后从所有候选（包括初始）中选择Q值最高的动作Â*。这实现每样本自适应β_eff。
  4. **残差速度匹配**：计算速度修正h_eff = (Â_base - Â*)/t_k，并由去噪时间门控s(t_k) = (1-t_k)^p加权。训练目标是用策略的当前速度v_θ去匹配[基础速度 + h_eff]，仅梯度流过当前预测，其余stop-gradient。
  5. **推理阶段**：直接使用微调后的速度场（v_θ = v_base + h_θ），无需测试时搜索或反向传播。
- **算法流程**（文字描述）：
  - 输入：策略v_θ、冻结基础策略v_base、冻结评论家Q、门控s(t)、梯度上升步数J、步长α。
  - 每次训练迭代：采样状态s和初始噪声x0；对K个去噪步：
    - 计算当前策略预测v_k；
    - 通过冻结基础策略前瞻得到Â_base；
    - 对Â_base执行J步Q梯度上升，得到候选集；
    - keep-best选择Â*；
    - 计算h_eff = (Â_base - Â*)/t_k；
    - 累加残差匹配损失 L += s(t_k) * ||(v_θ - v_base) - sg(h_eff)||²；
    - 用stop-gradient的当前策略前进到下一去噪状态。
  - 按损失更新θ。

## 3. 实验设计

- **数据集/场景**：
  - **LIBERO**：40个桌面任务，7自由度末端执行器动作空间，分为Spatial、Object、Goal、Long四个子套件。
  - **RoboTwin 2.0**：10个双臂操作任务，14自由度关节位置（qpos）控制，涵盖抓取、放置、协调、工具使用等。
  - **真实机器人**：两个桌面任务“Pick Peach”和“Stack Bowls”，7自由度臂，双RGB-D相机。
- **基准协议**：
  - 所有方法共用相同的**少样本SFT π0.5初始化**、相同的rollout数据集（由SFT策略收集）、相同的RLT特征和Cal-QL评论家。
  - 对比方法包括：
    - 测试时Q选择（Q Selection）：对多个采样动作按值排序选最优。
    - 测试时Q引导（Q Guidance）：推理时对采样动作做∇_A Q梯度精化。
    - Q引导动作蒸馏（Q Distillation）：将评论家改进的动作作为监督标签蒸馏到策略。
    - Diffusion-QL：直接反向传播Q通过去噪链。
- **评估指标**：单任务成功率（%），每个任务统计50次（LIBERO、RoboTwin 2.0）或20次（真实机器人）独立rollout。

## 4. 资源与算力

- **文中未明确说明**：论文未给出使用的GPU型号、数量、训练时长等具体算力信息。仅在附录提及评论家参数量~3.9M/头，以及RLT编码器等结构细节。可以推测该方法在单卡或多卡GPU上可运行，但具体资源未报告。

## 5. 实验数量与充分性

- **实验数量**：
  - LIBERO：40个任务，每个方法50次rollout → 约200×50 = 10000次仿真评估。
  - RoboTwin 2.0：10个任务，每个50次 → 500次评估。
  - 真实机器人：2个任务，每个20次 → 40次物理试验。
  - 消融实验（5个变体）在LIBERO上进行，涵盖评论家侧（3种）和策略侧（3种）共5种变体（文中报告5行数据，实际涉及两个类别共6种，但有一行重复？按表4有5行变体）。
- **充分性**：
  - **客观**：所有基线使用相同骨干、相同评论家、相同rollout数据，比较公平。
  - **覆盖范围**：涵盖仿真（单臂+双臂）和真实机器人，任务多样。消融覆盖了关键设计：评论家结构、状态表示、动作注入、集成；策略侧的keep-best、门控、冻结锚。
  - **统计可靠性**：每个结果基于50或20次卷绕，提供百分比，未给出方差或置信区间，但趋势清晰。
  - 缺少对超参数（如梯度上升步数J、步长α、门控指数p）的敏感性分析，以及不同rollout数据量下的表现。

## 6. 主要结论与发现

- Q-VGM在所有三个场景下全面优于所有同骨干、同评论家的基线。
  - **LIBERO**：成功率从SFT的75.0%提升至92.5%，超过测试时方法（82%~85%）和蒸馏方法（86.3%），Diffusion-QL反而下降至69.5%。
  - **RoboTwin 2.0**：从76.4%提升至87.2%，在困难任务（lift pot、handover mic、place shoe）上提升超过15个百分点。
  - **真实机器人**：从40.0%提升至67.5%，提升27.5个百分点。
- 消融证实：RLT编码器、每层动作注入、评论家集成、keep-best选择、门控s(t)、冻结基础锚均对最终性能有显著贡献；缺失任一项平均成功率下降3~10个百分点。

## 7. 优点

- **方法创新**：将VGG-Flow的价值梯度引导框架适配到机器人VLA微调，避免了通过去噪链反向传播的不稳定，也不需要动作似然，解决了流策略RL微调的长期难题。
- **实用范式**：仅需少量初始化演示（few-shot SFT）和自生成rollout数据，即可通过离线训练显著提升性能，无需在线交互或额外专家监督。
- **架构设计**：动作敏感评论家的每层动作注入、RLT状态压缩、Cal-QL及集成设计，保证了Q梯度在动作空间的有用性，并缓解了过估计问题。
- **鲁棒机制**：keep-best选择（自适应β_eff）和梯度裁剪有效防止评论家外推错误导致的过优动作，冻结基础锚保证前瞻估计在分布内。
- **实验全面**：跨仿真（单臂+双臂）和真实机器人验证，消融充分，对比公平。

## 8. 不足与局限

- **评论家泛化依赖**：梯度信号在离线rollout支持区域之外不可靠，虽然使用裁剪和keep-best缓解，但未内化信任区到采样动力学中（论文也提到此限制，并建议未来工作）。
- **可扩展性**：Q学习在长任务和复杂环境下扩展困难，论文建议结合世界模型缩短TD horizon，但未验证。
- **超参数未充分分析**：梯度上升步数J、步长α、门控指数p等关键超参数缺乏敏感性研究。
- **统计信息不足**：成功率仅报告均值，未提供方差/置信区间，无法判断差异显著性；真实机器人实验仅20次卷绕，统计可靠性稍弱。
- **基线覆盖有限**：仅比较了相同骨干的基线，未与更先进的在线RL方法（如PPO+近似似然）或其他离线RL算法（如IQL）比较。
- **算力资源未披露**：无法评估方法的计算成本。

（完）
