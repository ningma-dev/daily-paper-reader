---
title: Scaling World-Model Reinforcement Learning Through Diffusion Policy Optimization
title_zh: 通过扩散策略优化扩展世界模型强化学习
authors: "Xiaoyuan Cheng, Wenxuan Yuan, Zhancun Mu, Yuanzhao Zhang, Yiming Yang, Hai Wang, Zhuo Sun, Che Liu"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26282v1"
tags: ["query:world-models"]
score: 9.0
evidence: 基于扩散策略优化的世界模型强化学习，解决搜索与价值学习的不一致
tldr: 基于模型的强化学习面临模型偏差和误差累积问题，但更关键的瓶颈是搜索与价值学习之间的结构性错位，导致训练不一致。本文提出MBDPO框架，将策略优化重构为在潜在世界模型中对搜索轨迹的扩散过程，通过隐式能量函数锚定策略。实验表明，MBDPO在多任务离线预训练、在线学习和离线到在线微调中均表现优异，且离线预训练时模型容量增大性能持续提升。该工作统一了搜索与策略优化，为世界模型的可扩展策略学习提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26282v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-002.webp\", \"caption\": \"\", \"page\": 3, \"index\": 2, \"width\": 1099, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-003.webp\", \"caption\": \"\", \"page\": 3, \"index\": 3, \"width\": 383, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-004.webp\", \"caption\": \"\", \"page\": 3, \"index\": 4, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-005.webp\", \"caption\": \"\", \"page\": 3, \"index\": 5, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-006.webp\", \"caption\": \"\", \"page\": 6, \"index\": 6, \"width\": 5192, \"height\": 1650}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-007.webp\", \"caption\": \"\", \"page\": 6, \"index\": 7, \"width\": 4050, \"height\": 1399}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-008.webp\", \"caption\": \"\", \"page\": 21, \"index\": 8, \"width\": 1659, \"height\": 948}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-009.webp\", \"caption\": \"\", \"page\": 21, \"index\": 9, \"width\": 1644, \"height\": 957}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-010.webp\", \"caption\": \"\", \"page\": 22, \"index\": 10, \"width\": 1600, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-011.webp\", \"caption\": \"\", \"page\": 22, \"index\": 11, \"width\": 1991, \"height\": 789}]"
motivation: 现有基于模型的方法中搜索与价值学习存在结构性错位，导致训练不一致和次优性能。
method: 提出MBDPO，将策略优化视为潜在世界模型中对搜索轨迹的扩散过程，利用隐式能量函数锚定策略。
result: 在多任务离线预训练、在线学习及微调中表现优异，离线预训练时性能随模型容量增大单调提升。
conclusion: MBDPO通过扩散表示统一搜索与优化，有效缓解错位，具备良好可扩展性。
---

## 摘要
基于模型的强化学习通过使用世界模型可以在大规模上得到有效支持。然而，在实践中，扩展此类方法仍然受到根本性限制。一个普遍公认的挑战是模型偏差和误差累积，这会降低长程预测的质量。除了这些问题之外，我们识别出一个更关键但尚未被充分探索的瓶颈：现有世界模型方法中搜索与价值学习之间的结构错位。特别是，策略改进通常依赖于由独立的、非搜索策略导出的价值函数，导致训练不一致并最终导致次优学习。为了解决这一局限，我们提出了基于模型的扩散策略优化（MBDPO），这是一种在世界模型中统一搜索和策略优化的框架，通过扩散策略表示释放世界模型用于可扩展策略学习的潜力。我们不是在学习到的世界模型上构建显式规划器，而是将策略优化重新表述为潜在世界模型中搜索轨迹上的扩散过程。从这一视角出发，我们从收集的数据集中提取一个隐式能量函数来锚定策略，使MBDPO能够精修策略优化的评分场，同时缓解错位问题。我们在多种设置下评估MBDPO，包括多任务离线预训练、在线学习以及离线到在线微调。在离线模式下，我们进一步通过在大规模数据集上预训练来研究其扩展行为，观察到随着模型容量增加，性能持续单调提升。

## Abstract
Model-based reinforcement learning (RL) can be effectively supported at scale through the use of world models. However, in practice, scaling such approaches remains fundamentally limited. A commonly recognized challenge is model bias and error compounding, which degrade long-horizon predictions. Beyond these issues, we identify a more critical yet underexplored bottleneck: a structural misalignment between search and value learning in existing world model approaches. In particular, policy improvement often relies on value functions induced by a separate, non-search policy, resulting in training inconsistency and ultimately suboptimal learning. To address this limitation, we propose Model-Based Diffusion Policy Optimization (MBDPO) in world models, a framework that unifies search and policy optimization through diffusion policy representations, thereby unlocking the potential of world models for scalable policy learning. Instead of constructing an explicit planner over a learned world model, we reformulate policy optimization as a diffusion process over searched trajectories in latent world models. In this view, we extract an implicit energy function from the collected dataset that anchors the policy, enabling MBDPO to refine the score field for policy optimization while mitigating misalignment. We evaluate MBDPO across a wide range of settings, including multi-task offline pretraining, online learning, and offline-to-online fine-tuning. In the offline regime, we further investigate its scaling behavior by pretraining on large-scale datasets, observing consistent and monotonic performance gains with increasing model capacity.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对论文《通过扩散策略优化扩展世界模型强化学习》进行结构化、深入、客观的中文总结。

# 论文深度分析总结：《通过扩散策略优化扩展世界模型强化学习》(MBDPO)

## 1. 核心问题与整体含义（研究动机与背景）

*   **核心问题**：基于世界模型的强化学习方法（如TD-MPC系列）虽然前景广阔，但在扩展至更大规模时面临根本性瓶颈。此前公认的瓶颈是模型误差累积，但本文指出一个更深层次、未被充分探索的结构性问题：**策略搜索（Search）与价值学习（Value Learning）之间的结构性错位**。
*   **具体机制**：
    *   现有方法（如TD-MPC）中，价值函数（Q函数）是基于一个“非搜索策略”（如由参数化网络给出的策略）生成的轨迹进行训练的。
    *   然而，策略改进（或规划）是通过一个“基于搜索的规划器”（如MPPI）在世界模型中搜索高价值轨迹来实现的。
    *   这种分布不一致导致了一个恶性循环：价值函数在搜索策略访问的分布外（OOD）状态-动作对上产生不可靠的过高估计；搜索策略则会利用这些虚假的“高峰值”，从而放大价值估计误差，最终导致性能下降和无法扩展。
*   **研究意义**：本文旨在解决这一结构性错位，释放世界模型在可扩展策略学习方面的潜力，为构建大规模、自我完善的通用智能体奠定基础。

## 2. 方法论：模型基础扩散策略优化

*   **核心思想**：将策略优化重构为一个**扩散过程**。具体来说，不是在学到的世界模型上运行一个**显式**的规划器（如MPPI），而是将策略改进视为一个**隐式**的、多步去噪的过程，该过程直接在世界模型的潜在空间中对搜索轨迹进行优化。这统一了搜索与价值学习，从根本上消除了结构性错位。

*   **关键技术细节**：
    *   **统一搜索与策略优化**：将最优策略定义为满足KL散度约束的优化问题解，即最大化预期回报，同时限制搜索策略与行为策略（β）的KL散度，防止策略漂移。
    *   **隐式能量函数**：利用对比学习从经验池数据中学习一个隐式能量函数 \( E_\theta \)。该能量函数近似于行为策略的负对数概率 \( (-E(z, a) \approx \log \beta(a|z) + C(z)) \)。这个能量项被吸收到奖励函数中，实现了策略更新的“锚定”，使其不偏离行为策略。
    *   **扩散策略**：将动作序列的生成视为一个去噪扩散概率模型（DDPM）的逆过程。关键在于，扩散模型的**评分函数** \( \phi \) 不依赖于外部数据，而是通过世界模型想象出的轨迹进行估计。
    *   **世界模型即评分场修正器**：从高斯噪声开始，每一步去噪都利用世界模型评估一批动作序列的回报，并据此赋予权重。这些加权后的动作序列被用来估算评分函数，指导去噪方向，使最终生成的策略倾向于高回报区域。世界模型在此过程中扮演了策略优化的评分场修正器角色。
    *   **理论保证**：论文证明，当评分匹配条件满足时，扩散策略得到的最优解与KL约束优化问题的Gibbs分布解完全等价。这表明该流程是一个有原则的策略优化方法，而不仅仅是启发式搜索。

## 3. 实验设计

*   **数据集/场景**：
    *   **模拟环境基准测试**：涵盖了4个主流基准测试中的**121个**连续控制任务，包括DMControl、MetaWorld、ManiSkill2和MyoSuite。任务难度跨度极大，包括高维控制、稀疏奖励、人体工学控制和视觉控制（Visual RL）等。
*   **评估设置模式**：涵盖了三种主要RL范式：
    *   在线从头学习
    *   多任务离线预训练
    *   离线到在线微调。
*   **对比方法**：
    *   SAC（模型无关方法）
    *   DreamerV3（世界模型方法）
    *   TD-MPC（世界模型方法）
    *   TD-MPC2（世界模型方法，作为主要对比基线）

## 4. 资源与算力

*   论文明确提供了计算资源信息：
    *   **在线训练**：在单个NVIDIA A800-80GB GPU上进行，按每环境步的毫秒数计算。
    *   **离线训练**：在单个NVIDIA H200 GPU上进行，报告了10M训练步骤的总时长（小时）。
*   **具体数值**（部分示例）：
    *   **在线训练**：MBDPO在MetaWorld上的时间成本为1.08毫秒/步，与TD-MPC2 (1.08)持平；在Visual RL上，MBDPO的3.04毫秒/步甚至低于TD-MPC2的3.55毫秒/步。
    *   **离线多任务训练**：340M参数的模型，在30/80个任务上训练时间约为203/207小时。MBDPO相较于TD-MPC2的额外开销非常小（最多1.32倍），表明其具有很高的计算效率。

## 5. 实验数量与充分性

*   **实验数量巨大**：论文在**121个任务**上进行了系统评估，实验数据点极多。
*   **实验充分性**：
    *   **全面覆盖**：覆盖了多种主流RL范式和几乎所有主流基准测试，具有很强的代表性。
    *   **消融研究充分**：
        1.  **能量正则化因子 \( \eta \)**：系统测试了 \( \eta \) 从0到5的范围，揭示了其重要性及最佳区间。
        2.  **扩散步骤与蒙特卡洛样本数**：分别在DMControl和MetaWorld的多个挑战性任务上，测试了不同数量的扩散步数和蒙特卡罗样本对性能和运行时间的影响，验证了理论误差界。
    *   **公平对比**：在所有对比方法上使用了相似或相同的训练配置和框架（如遵循TD-MPC的架构设计），在相同基准上进行了对比，实验结果客观、可信。
    *   **机制验证**：通过监测交叉TD误差和策略漂移（Action Drift）来实证验证“错位”问题的存在及其缓解。

## 6. 主要结论与发现

1.  **性能卓越**：MBDPO在在线从头学习、多任务离线预训练和离线到在线微调所有设置中，均**一致且显著优于**所有对比基线。
2.  **可扩展性强**：在多任务离线预训练中，随着模型参数从1.7M增加到340M，MBDPO的性能呈现**单调且清晰的扩展曲线**，这证明了其方法学在大规模场景下的有效性，而TD-MPC2的扩展曲线则相对更平缓。
3.  **有效缓解关键瓶颈**：通过实证分析（交叉TD误差和策略漂移），MBDPO确实缓解了搜索与价值学习之间的结构性错位问题。其扩散策略优化的框架提供了更稳定、更少漂移的训练过程。
4.  **隐式能量函数至关重要**：消融实验表明，引入隐式能量函数（\( \eta \neq 0 \)）是取得最佳性能的关键。没有它（\( \eta = 0 \)）性能会显著下降，但即便在此情况下，MBDPO的性能仍优于TD-MPC2。
5.  **物理对齐表示**：通过可视化潜在轨迹，MBDPO学到的策略展现出了与物理直觉高度一致的、有结构的轨迹（如循环任务中的闭环结构，操作任务中平滑的目标导向轨迹），而对比方法则显得混乱且缺乏物理意义。

## 7. 优点

1.  **方法原创性强**：将扩散模型与基于世界模型的RL进行了优雅而深刻的结合，提出了一种新的范式，直接解决了现有方法的根本性缺陷。
2.  **理论扎实**：论文不仅有精湛的算法设计，还提供了严谨的理论分析（问题1和2的证明，定理1的等价性），为方法的有效性提供了坚实的数学基础。
3.  **实验评估全面且系统**：覆盖了多种实验场景、大量任务，并进行了详尽的消融研究，展示了方法在不同条件下的鲁棒性和有效性，论证充分。
4.  **可扩展性验证**：不仅证明了性能的提升，还特别验证了模型在大规模下性能的**单调提升**，这是RL领域一直追求但难以实现的目标，具有里程碑意义。
5.  **计算效率高**：尽管增加了扩散模型，但MBDPO的训练效率与基线方法TD-MPC2相当，并无显著计算开销，表明其工程实现是高效的。

## 8. 不足与局限

1.  **模拟环境局限**：所有实验均在模拟环境中进行。虽然任务数量众多，但未来需要将其部署到真实世界的机器人上，以检验其鲁棒性和实际可用性。
2.  **扩展极限未充分探索**：受限于计算资源，论文没有将MBDPO扩展到更大规模（如数十亿参数）的“基础模型”规模。其完整的扩展潜力有待进一步探索。
3.  **异常任务分析**：在“Hopper Hop”任务上，MBDPO的离线到在线微调效果不佳。论文将其归因于该任务的潜在嵌入与其他训练任务存在严重分布偏差。这揭示了对任务分布敏感这一潜在风险，特别在目标任务分布严重偏离预训练分布时可能出现问题。
4.  **理论分析依赖较强假设**：理论分析中的某些部分（如行为策略的密度、KL散度界的计算）依赖于理想化的假设方法，简化了分析。在实际实现中，这些要素是通过近似和学习得到的，可能存在理论上未完全覆盖的误差源。

（完）
