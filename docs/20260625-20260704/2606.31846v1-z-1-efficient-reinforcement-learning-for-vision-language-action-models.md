---
title: "Z-1: Efficient Reinforcement Learning for Vision-Language-Action Models"
title_zh: Z-1：面向视觉-语言-动作模型的高效强化学习
authors: "Lang Cao, Renhong Chen, Luyi Li, Peng Wang, Mofan Peng, Yitong Li"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31846v1"
tags: ["query:robot-rl"]
score: 9.0
evidence: 使用GRPO的VLA模型强化学习后训练框架
tldr: "现有视觉-语言-动作模型多依赖行为克隆，难以从失败中改进。Z-1提出基于GRPO的强化学习后训练框架，在公开演示数据上微调后对24个任务进行任务级策略优化，通过共享前缀rollout、轨迹分支、奖励校准等技巧提升效率。在RoboCasa任务上平均成功率80.6%，相比SFT提升13.2%，超越现有最优模型。证明系统化RL后训练能有效提升流式VLA策略。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31846v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1411, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31846v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1376, \"height\": 478, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31846v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1442, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31846v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 437, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31846v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 965, \"label\": \"Table\"}]"
motivation: 现有VLA策略通过行为克隆从固定演示学习，缺乏从自身失败中改进的机会，性能受限。
method: 基于π0.5，先对公开RoboCasa演示进行SFT，再对24个任务应用任务级GRPO，结合共享前缀rollout、树结构轨迹分支、完成感知奖励校准和选择性联合训练。
result: "在全部24个RoboCasa任务上平均成功率80.6%，比SFT初始化提升13.2%，并超过已有最佳模型。"
conclusion: 系统化的GRPO后训练可显著提升流式VLA策略性能，无需额外私有演示。
---

## 摘要
视觉-语言-动作（VLA）模型通过连接语言指令、视觉观测和连续控制，为机器人操作提供了一个有前景的框架。然而，现有大多数策略仍受限于行为克隆或基于固定示范的监督微调（SFT），这使得从策略自身的失败中改进的机会有限。本文提出了Z-1，一种基于流的VLA模型的强化学习（RL）后训练框架。Z-1构建于π_{0.5}之上，仅使用公开的RoboCasa演示数据进行SFT，然后在24个标准RoboCasa任务上应用任务级组相对策略优化（GRPO）策略。为了提升在线优化的效率和稳定性，Z-1结合了共享前缀轨迹生成、树结构轨迹分支、完成感知奖励校准以及VLM与动作专家选择性联合训练。在所有24个RoboCasa任务上，Z-1实现了80.6%的平均成功率，相比其SFT初始化提升了13.2个百分点，并且超越了已发表的最先进模型。这些结果表明，系统的GRPO后训练可以显著提升基于流的VLA策略，而无需额外的私有演示数据。

## Abstract
Vision-Language-Action (VLA) models offer a promising framework for robotic manipulation by connecting language instructions, visual observations, and continuous control. However, most existing policies remain limited by behavior cloning or supervised fine-tuning (SFT) from fixed demonstrations, which provides limited opportunity to improve from the policy's own failures. In this paper, we present Z-1, a reinforcement learning (RL) post-training framework for flow-based VLA models. Built on top of $π_{0.5}$, Z-1 uses only publicly released RoboCasa demonstrations for SFT and then applies a task-wise Group Relative Policy Optimization (GRPO) strategy across $24$ standard RoboCasa tasks. To improve the efficiency and stability of online optimization, Z-1 combines shared-prefix rollout construction, tree-structured trajectory branching, completion-aware reward calibration, and selective joint training of VLM and Action Expert. Across all $24$ RoboCasa tasks, Z-1 achieves an average success rate of $80.6\%$, improving over its SFT initialization by $13.2\%$ points and outperforms the published sota models. These results show that systematic GRPO post-training can substantially improve flow-based VLA policies without additional private demonstrations.

---

## 论文详细总结（自动生成）

# Z-1：面向视觉-语言-动作模型的高效强化学习——论文详细总结

## 1. 论文的核心问题与整体含义

- **研究动机**：现有视觉-语言-动作（VLA）模型主要依赖行为克隆（BC）或监督微调（SFT）从固定演示中学习，缺乏从自身失败中改进的能力。在长时域和组合任务中，微小预测误差会累积，导致策略进入训练数据覆盖不足的状态，性能受限于演示覆盖范围。
- **核心问题**：如何利用强化学习（RL）后训练高效、稳定地提升基于流的VLA策略，且仅使用公开演示数据，无需额外私有数据。
- **整体含义**：提出的Z-1框架证明，系统性的GRPO后训练可以显著提升流式VLA策略在模拟操作基准上的性能，为机器人学习中的RL后训练提供了实用方法。

## 2. 论文提出的方法论

### 核心思想
采用两阶段训练：先进行场景级SFT初始化，再对每个任务独立进行GRPO后训练。通过多种模块化设计提升RL效率和稳定性：共享前缀rollout、树结构轨迹分支、完成感知奖励校准、选择性VLM-动作专家联合训练。

### 关键技术细节

1. **两阶段训练**：
   - **Stage1: 场景级SFT**：基于预训练π₀.₅，使用1199个公开RoboCasa演示，按场景类别训练（而非按任务），提高数据效率。
   - **Stage2: 任务级GRPO后训练**：从对应场景SFT检查点开始，对24个任务逐一优化，避免跨任务梯度干扰。

2. **GRPO优化器**：
   - 从SFT初始化πθ出发，对每个训练实例采样G个rollout，计算组相对优势：  
     Aᵢ = (Rᵢ - μR) / (σR + ε)  
   - 优化裁剪后的组相对目标函数（公式3），使用流-SDE公式计算似然比（附录D.3）。

3. **共享前缀GRPO**：
   - 让组内所有rollout共享一个共同的前缀（approach phase），仅从分支点开始独立采样后缀。前缀动作被屏蔽，不参与梯度更新，减少冗余计算并聚焦于精细操作阶段。

4. **树结构前缀分支**：
   - 泛化共享前缀：在prefix内设置中间分支点，形成多级层次树。只有完全共享的根段被屏蔽，部分共享段仍可训练，减少过度屏蔽，提升学习信号。

5. **完成感知奖励校准（Success-Aware Reward Decay）**：
   - 对成功轨迹的奖励按完成步数指数衰减：R̃ᵢ = r·γ^(dᵢ)（γ=0.998）。
   - 全部成功的组使用非负排序优势（Aᵢ = Rᵢ - minⱼRⱼ），避免惩罚较慢的成功轨迹。

6. **选择性VLM-动作专家联合训练（Sel-JT）**：
   - 默认只更新动作专家（AE-only）；若训练诊断表明视觉-语言表示不足（低SFT成功率、感知相关失败），则同时更新VLM骨干。配置在最终评估前确定，不利用评估rollout选择。

## 3. 实验设计

- **数据集/场景**：RoboCasa模拟基准，包含7个场景类别（门、抽屉、拾放、微波炉、水槽/水龙头、炉子、咖啡制作），共24个标准操作任务。
- **基准方法**：GR00T、GR00T N1.5、Video Generators are Robot Policies、X-WAM（均为已发表RoboCasa结果）。
- **评估指标**：任务成功率（%），每个任务进行48-64次独立评估rollout（SFT：48次，RL：64次）。
- **实验设置**：基于π₀.₅，SFT使用1199个公开演示；RL阶段使用64个并行训练环境，每步1024全局batch，组大小8（树分支时组大小为4，同时增大batch以保持有效组数）。

## 4. 资源与算力

论文**未明确说明**使用的GPU型号、数量及总训练时长。仅提及RL训练使用AdamW优化器，学习率5e-6，梯度裁剪1.0，无额外KL正则化。附录C.3提供了超参数详情，但未提及计算资源具体配置。

## 5. 实验数量与充分性

- **覆盖范围**：在全部24个RoboCasa任务上进行了完整评估，覆盖面广。
- **消融实验**：进行了三组消融分析：
  1. 共享前缀与树分支（TurnOnSinkFaucet任务）；
  2. 选择性VLM-动作专家联合训练（TurnOnStove任务）；
  3. 完成感知奖励校准 vs. 惩罚 vs. 标准GRPO（PnPStoveToCounter任务）。
- **公平性**：与四个已发表方法对比（取各自论文中报告的数字），而非完全复现；Z-1自身使用相同公开演示，与X-WAM（引入了深度信息）对比时强调公平性。
- **充分性**：实验设计合理，消融实验针对关键模块设计，但消融仅在少数代表任务上进行，未在所有24个任务上逐一验证各模块效果。此外，树分支存在稳定性权衡，未进行多随机种子下的统计显著性检验。

## 6. 论文的主要结论与发现

- Z-1 RL在24个任务上平均成功率80.6%，比SFT初始化（67.4%）提升13.2个百分点，超越所有对比的已发表方法（X-WAM 79.2%）。
- 提升在接触密集型任务（抽屉、水槽/水龙头）最为显著（分别从83.4%→96.1%，63.2%→94.3%）；炉子任务仍具挑战（32.8%），提示长时域推理和鲁棒恢复仍需改进。
- 共享前缀和树分支能提供更清晰的组内比较信号；完成感知奖励校准在引入时间信息的同时避免过度惩罚；选择性联合训练对感知依赖强的任务至关重要。

## 7. 优点

- **模块化设计**：各模块可独立启用/组合，通过训练诊断自动选择，灵活适应不同任务需求。
- **效率提升**：共享前缀减少冗余rollout计算；树分支保留更多可训练段；避免使用额外奖励模型或KL正则化。
- **稳定性**：共享前缀提供保守但稳定的学习信号；完成感知奖励的非负排序避免对成功轨迹的负面惩罚。
- **公开数据友好**：完全依赖公开演示，无需私有数据，可复现性强。
- **广泛评估**：覆盖24个任务，与四个SOTA基线对比，结果具有说服力。

## 8. 不足与局限

- **炉子任务表现弱于X-WAM**：表明长时域运输、空间推理和中间失败恢复仍是难点。
- **消融实验覆盖不全**：仅对单个任务进行消融，未在所有类别上验证各模块泛化性。
- **树分支稳定性问题**：论文自身承认树分支可能引入训练不稳定，“在实验中有时导致训练动态不稳定”。
- **无真实世界验证**：所有实验在模拟器中进行，未在真实机器人上部署验证。
- **计算资源未披露**：无法评估方法的经济性及可扩展性。
- **评估偏差风险**：未明确报告多随机种子下的标准差；成功率基于48-64个rollout，统计误差未量化。
- **选择性联合训练的决策准则**：依赖“训练诊断”，但未给出具体阈值或自动化规则，依赖人工判断，可能影响可复现性。

（完）
