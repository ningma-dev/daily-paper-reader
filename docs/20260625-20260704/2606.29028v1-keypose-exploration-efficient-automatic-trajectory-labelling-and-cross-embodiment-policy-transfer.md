---
title: "Keypose Exploration: Efficient Automatic Trajectory Labelling and Cross-Embodiment Policy Transfer"
title_zh: 关键姿态探索：高效自动轨迹标注与跨体策略迁移
authors: "Yupu Lu, Hang Xu, Yizhou Chen, Jia Pan"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.29028v1"
tags: ["query:vla-policy"]
score: 9.0
evidence: 使用VLM进行轨迹标注并训练关键点引导的扩散策略用于操作
tldr: 现有基于关键点的操作依赖人工标注，效率低。本文提出自动轨迹标注流程，仅对每个任务的一个演示使用VLM进行语义事件检测，结合轨迹分析实现时间对齐，从而自动提取关键点。利用标注数据训练关键点引导的扩散策略，该策略通过可达性地图过滤候选关键点，引导策略适应目标机器人，实现零样本跨实体迁移。在robomimic任务上，自动标注策略性能与标准扩散策略持平，且可达性过滤的关键点在多模态插入任务上能提升迁移效果。该工作为自动化关键点标注和跨实体策略迁移提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29028v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1831, \"height\": 1001, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29028v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 674, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29028v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 1362, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29028v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 795, \"height\": 183, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29028v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 846, \"height\": 533, \"label\": \"Table\"}]"
motivation: 现有方法依赖人工标注关键点，效率低且难以跨实体迁移。本文提出自动标注流程，仅需每个任务单个演示的VLM推理，并探索关键点引导策略的跨实体迁移潜力。
method: 结合VLM语义事件检测和轨迹时间对齐自动标注关键点；训练关键点引导的扩散策略，通过可达性地图过滤候选关键点实现零样本跨实体迁移。
result: 在robomimic任务上，自动标注策略性能与标准扩散策略相当；可达性过滤的关键点条件在插入任务上能提升零样本跨实体迁移效果。
conclusion: 提出高效自动关键点标注方法，验证关键点引导策略具有跨实体迁移可行性，为通用操作策略学习奠定基础。
---

## 摘要
基于关键姿态的操作将任务分解为关键路径点，以简化长时域任务的策略学习，但现有方法依赖任务特定的启发式规则或人工标注从演示中提取关键姿态。我们提出了一种针对抓取相关任务的自动轨迹标注流程。该流程结合了视觉语言模型（VLM）进行语义事件检测与经典轨迹分析实现精确时间对齐，仅需对每个任务中重复演示中的一个演示进行VLM推理。利用标注数据，我们训练了一种关键姿态引导的扩散策略（DP），该策略利用关键姿态条件来干预演示分布。我们探索了将此属性应用于跨体迁移的可能性：通过可达性地图对候选关键姿态进行采样和过滤，引导策略朝向目标机器人的运动学可行关键姿态。作为初步可行性研究，在两个robomimic任务上的实验表明，标注数据生成的策略与标准DP基线相当，并且在多模态插入任务中，当存在可行候选时，可达性过滤的关键姿态条件可能有利于零样本迁移。

## Abstract
Keypose-based manipulation decomposes tasks into critical waypoints to simplify policy learning for long-horizon tasks, but existing approaches rely on task-specific heuristics or manual annotation to extract keyposes from demonstrations. We present an automatic trajectory labelling pipeline for grasp-related tasks. This pipeline combines vision-language models (VLMs) for semantic event detection with classical trajectory analysis for precise temporal alignment, requiring VLM inference only on one single demo among repeating ones per task. Using the labelled data, we train a keypose-guided Diffusion Policy (DP) that exploits keypose conditioning to intervene demonstration distributions. We explore the possibility to apply this property for cross-embodiment transfer: candidate keyposes are sampled and filtered via a reachability map, steering the policy toward kinematically feasible keyposes for the target robot. As a preliminary feasibility study, experiments on two robomimic tasks show that the labelled data produces policies matching a standard DP baseline, and that reachability-filtered keypose conditioning may benefit zero-shot transfer on the multimodal insertion task when feasible candidates are available.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：基于关键姿势（keypose）的操作方法能够将长时域任务分解为关键路径点，简化策略学习。然而，现有方法提取关键姿势时要么依赖任务特定的启发式规则（如速度阈值、夹爪状态变化），要么需要人工手动标注，导致效率低下、缺乏通用性，难以扩展到大规模数据集。此外，如何利用关键姿势进行跨体（cross-embodiment）策略迁移尚是开放问题。
- **整体目标**：提出一种**自动的轨迹标注流程**，仅需对每个任务的一条演示使用视觉语言模型（VLM）进行语义事件检测，结合经典轨迹分析实现精确时间对齐，从而自动生成关键姿势标签。并基于这些标签训练**关键姿势引导的扩散策略**，通过可达性地图过滤候选关键姿势，实现零样本跨体迁移。论文将其定位为初步可行性研究。

## 2. 论文提出的方法论

### 核心思想
- 将“关键姿势”定义为抓取类操作中与交互时刻（如抓取、释放、插入、分离、着陆）对应的末端执行器位姿。
- 采用**两阶段自动标注流程**：
  1. **VLM 引导的子任务分析**：利用 Qwen3-VL-235B 对一条演示视频（均匀采样 RGB 帧）进行推理，识别子任务阶段（接近、交互、结果）及事件类型（grasp/release/detach/interact/land），输出结构化 JSON 包含帧时间戳。
  2. **基于轨迹的自动校正**：对原始轨迹进行分层运动分割（双层层析法），构建运动时间轴和夹爪时间轴，然后应用语义-运动对应规则将 VLM 估计的事件帧“吸附”到物理运动边界和夹爪转换点，得到精确的关键姿势帧。
- 训练**关键姿势预测网络**：采用一致性模型（Consistency Model）作为单步预测器，从当前观测直接预测下一个关键姿势。
- 训练**关键姿势引导的扩散策略**：将预测的关键姿势作为条件，与观测拼接后输入 Transformer 去噪网络，生成动作轨迹。
- **跨体迁移**：利用预计算的可达性地图（RichMap）过滤预测的候选关键姿势，仅保留目标机器人运动学可行的姿势，实现零样本迁移（propose-filter-generate 流程）。

### 关键技术细节
- **VLM 提示设计**：将任务分解为“接近→交互（含多种原语）→结果”三段式结构，引导 VLM 输出事件类型及其帧号。
- **运动分割**：首先对每个笛卡尔轴进行 Savitzky-Golay 平滑，使用速度阈值标记运动方向（+1/-1/0），独立分段；然后跨轴合并，保留方向反转边界，丢弃短片段；可选第二层检测细微运动（慢速接近）。
- **对应规则**：例如 grasp 对应夹爪闭合首帧；interact 对应释放前最后一个运动相的首帧；detach 对应夹爪闭合结束与抓取后首次运动的较大帧。
- **关键姿势向量**：包含位置 p（3D）、6D 连续旋转表示、2 指关节位置，共 11 维，归一化到[-1,1]。
- **关键姿势预测**：训练一致性模型，损失为一致性蒸馏损失（伪 Huber 度量）加上子任务阶段交叉熵辅助损失。
- **策略训练**：标准扩散策略噪声预测损失，条件 c = [o; k]。
- **迁移**：采样 24 个候选关键姿势 → 聚类为最多 3 个方向模式 → 查询可达性地图 → 选择候选数量多且置信度最高的模式中最佳可达候选；若无可达候选则回退至最高置信度候选。

### 公式/算法流程（文字说明）
- 运动分割：$\tilde{p}^k_t = \text{SavGol}(p^k_t; w, d)$, $v^k_t = (\tilde{p}^k_{t+1} - \tilde{p}^k_t)/\Delta t$, 标签 $\ell^k_t \in \{+1,-1,0\}$ 根据阈值判断。
- 关键姿势损失：$L_{kp} = E_{k_0, t} [d(f_\theta(k_t, t, f_{obs}), f_{\theta^-}(k_{t'}, t', f_{obs}))]$
- 扩散策略损失：$L_{dp} = E_{t,o,k,\tau_0,\epsilon} [\| \epsilon - \epsilon_\theta(\tau_t, t, c) \|^2]$
- 迁移：$p_{\text{target}}(k|o) \propto S(k; \text{target}) \cdot p_{\text{source}}(k|o)$

## 3. 实验设计

### 数据集/场景
- **robomimic 环境**中的两个任务：
  - **Can**：抓取-放置任务（固定放置位，单模态演示分布）。
  - **Square**：方形螺母插入方柱（三种不同插入方向，多模态演示分布）。
- 所有模型仅在 **200 条 Franka Panda 演示**上训练。
- 测试零样本迁移至两种目标机器人：**Kinova3** 和 **UR5e**。
- 为避免视觉域偏移，所有观测输入采用低维状态向量（23 维，包括物体状态和末端位姿），而非图像。

### Benchmark 与对比方法
- **DP**：标准扩散策略（基线）。
- **KP**：本文关键姿势引导策略，迁移时不使用可达性过滤。
- **KP+Reach**：KP 加上 propose-filter-generate 管道（使用可达性过滤）。

### 评估指标
- **成功率**（Success Rate）均值±标准差（9 个 checkpoint：3 个训练种子 × 3 个检查点 epoch，每个 checkpoint 评估 50 个 episode）。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量、训练时长。
- 提及了关键姿势预测网络参数为 0.37M，扩散策略 Transformer 参数为 8.97M。
- VLM 使用了 Qwen3-VL-235B（235B 参数），但仅对每个任务的一条演示进行一次推理，开销可控。
- 所有实验在模拟器中进行。总体计算资源需求不高，但作者未提供具体硬件信息。

## 5. 实验数量与充分性

- **实验数量**：两个任务（Can、Square）× 三种方法（DP、KP、KP+Reach）× 三种机器人（Panda、Kinova3、UR5e）= 共 3×3×2=18 种条件组合。每个条件 9 个 checkpoint × 50 episodes = 450 次评估，总计约 8100 次 rollout。
- **充分性分析**：
  - **优点**：覆盖了单模态和多模态任务、源机器人和两种不同目标机器人；对比了有无可达性过滤的消融；统计了方差。
  - **不足之处**：
    - 仅两个模拟任务，且均为短时域（1~2 个交互事件），未能充分体现关键姿势对长时域任务的分解优势。
    - 未与其它自动标注方法（如基于启发式的自动提取）进行对比。
    - 未进行视觉输入下的实验（刻意避免域偏移，但也限制了通用性）。
    - 未包含真实机器人实验。
    - 每个任务仅使用 200 条演示，规模较小。
  - **公平性**：观测输入统一为低维状态，避免视觉差异；所有模型使用相同演示数据；超参数除部分任务特定（平滑窗口、阈值）外尽量共享。对比设置客观。

## 6. 论文的主要结论与发现

1. **自动标注质量可行**：在源机器人 Panda 上，KP 和 KP+Reach 的成功率与 DP 基线相当（Can: 100% vs 98.7%；Square: 84.2%~87.3% vs 86.2%），证明自动生成的标签能够训练出有效策略。
2. **关键姿势条件可以暴露多模态**：在 Square 任务上，DP 迁移至 UR5e 时方差较大（73.8%±10.3%），而 KP 将其稳定至 75.8%±5.5%，KP+Reach 进一步提升至 82.9%±4.4%，接近源机器人性能（86.2%）。这表明关键姿势条件帮助策略选择特定插入模式，且当可达候选充足时，可达性过滤能有效引导策略适应目标机器人。
3. **可达性过滤的效果受限于候选可行性**：UR5e 在 Square 上几乎所有状态都能找到可达候选（约 99%），因此过滤有明显收益；而 Kinova3 的可达性极差（~0.1%），过滤几乎退化为回退机制，导致 KP+Reach 与 KP 无明显差异（24.2% vs 18.7%，均低于 DP 的 22.0%，但差异不显著）。
4. **对单模态任务（Can）**：关键姿势条件未带来增益，反而有轻微干扰（KP 略低于 DP），但差距很小。
5. **保守行为**：观察发现关键姿势引导的策略在接近关键姿势时动作更谨慎，有助于减少对齐误差。

## 7. 优点

- **自动化程度高**：仅需每个任务一条演示的 VLM 推理，其余演示自动传播标注，显著减少人工成本。
- **结合语义与物理线索**：VLM 提供高层次语义理解，轨迹分析提供精确时间对齐，互补性强。
- **关键姿势条件具有迁移价值**：通过可达性过滤实现了零样本跨体迁移，无需重新训练或微调，为策略泛化提供了新思路。
- **清晰的消融设计**：对比有无可达性过滤、不同机器人，系统验证了关键姿势条件的作用条件（依赖候选可行性）。
- **代码和实验细节较完整**：提供了任务参数、网络结构、损失函数等细节，可复现性较好。

## 8. 不足与局限

- **任务范围有限**：仅适用于抓取类操作（grasp/release/detach/interact/land），无法处理推动、旋转、擦拭、倾倒等连续接触或非抓取操作。关键姿势表示在此类任务中缺乏自然定义。
- **参数依赖**：自动标注仍需人为设定每个任务的平滑窗口和速度阈值（尽管其他参数全局共享），未实现完全免参数。
- **工作流假设**：要求所有演示遵循相同的子任务顺序且无错误（重试、犹豫、顺序颠倒会破坏假设），基于规则的语义-运动对应无法处理冲突。
- **实验规模小**：仅在两种模拟任务、三种机器人上验证，缺乏真实机器人实验和多任务、长时域（>3个交互事件）验证，结果仅为初步可行性。
- **未考虑视觉域偏移**：刻意使用低维状态，规避了真实跨体迁移的核心挑战（视觉差异）。若要用于实际，需集成视觉适应技术。
- **关键姿势作为软条件**：而非硬约束，在目标机器人运动学严重不匹配时（如 Kinova3）几乎无效，需要更强的约束或数据增强才能克服大间隙。
- **未与其它自动标注方法对比**：例如基于启发式的自动提取（gripper state/velocity thresholds），无法直接评估本文相对优势。
- **算力信息缺失**：未报告训练 GPU 型号、时长等，影响复现成本评估。

（完）
