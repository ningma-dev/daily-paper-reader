---
title: Direct Action-Head Injection of A Grounded 3D Point Unlocks Spatial and Task Generalization
title_zh: 将接地3D点直接注入动作头实现空间与任务泛化
authors: "Shiang-Feng Tsai, Jin-Cheng Jhang, Yen-Ling Tai, Jia-Hong Lai, Shih-Yun Wong, KangTung-Hsu, Yi-Ting Chen"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27663v1"
tags: ["query:vla-policy"]
score: 9.0
evidence: 直接针对VLA模型的机器人控制，提升空间和任务泛化
tldr: 现有VLA模型在空间和任务泛化上表现脆弱，因为缺乏有效的定位信息注入。本文发现，将3D点云信息直接注入动作头是关键。提出轻量级模块，通过3D表示定位信号并计算与夹爪的相对位移，利用自适应层归一化注入动作头。无需改变主干网络，在LIBERO-PRO上，GR00T-N1.6的任务泛化成功率从31.2提升至77.5，空间泛化从28.1提升至60.2，且对π0.5同样有效。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27663v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 665, \"height\": 878, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 629, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1233, \"height\": 367, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 706, \"height\": 420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 777, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 733, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 731, \"height\": 407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 1260, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1440, \"height\": 1611, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1445, \"height\": 1270, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27663v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1301, \"height\": 594, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1083, \"height\": 363, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 489, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 491, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1213, \"height\": 1857, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1080, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1083, \"height\": 515, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1438, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1439, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1435, \"height\": 222, \"label\": \"Table\"}]"
motivation: VLA模型在测试时面对物体位置变化或指令变化时泛化能力差，现有2D像素或语言提示方法不足以解决。
method: 设计一个两层MLP模块，将3D定位点与夹爪的相对位移编码为空间嵌入，通过自适应层归一化直接注入动作头，无需修改骨干网络。
result: "在LIBERO-PRO基准上，GR00T-N1.6任务泛化提升46.3%，空间泛化提升32.1%；π0.5也获得类似增益。"
conclusion: 将3D定位信号直接注入动作头是解锁VLA空间和任务泛化的关键，且可通过轻量模块实现，主干无关。
---

## 摘要
视觉-语言-动作（VLA）模型利用大规模视觉-语言预训练实现灵活的机器人操作，但在测试时，它们仍会在两个维度上表现脆弱：空间泛化（当物体位置与训练时不同）和任务泛化（当熟悉场景与训练时不同的语言指令配对）。越来越多的方法通过赋予策略空间和任务感知信息（如用于物体定位和放置的2D像素坐标）来解决这一脆弱性。然而，我们发现通过语言提示或视觉提示的现有表示并未解决限制；相反，利用基于3D点的表示并将其直接馈送到动作头会带来显著改进——揭示了接地信号如何表示并注入VLA才是真正的变革者。因此，我们提出了一个轻量级、与模型无关的模块，该模块以3D形式表示接地信号，计算其相对于夹爪的位移，并通过自适应层归一化将所得空间嵌入直接注入动作头。整个模块是一个两层MLP，无需更改VLA主干或预训练流程。在LIBERO-PRO上，我们的方法在任务扰动下将GR00T-N1.6的平均成功率从31.2提高到77.5点，在位置扰动下从28.1提高到60.2点（增益分别为46.3和32.1点）。对于π0.5也实现了可比的增益，表明该机制与主干无关。这些结果共同支持了我们的核心发现：当接地信号提升到3D后，将其直接注入动作头是解锁VLA中空间和任务泛化的关键——只需在预训练主干的顶部添加一个轻量级模块即可实现。

## Abstract
Vision-Language-Action (VLA) models leverage large-scale vision-language pretraining for flexible robot manipulation, yet at test time they remain brittle along two axes: spatial generalization, when object positions differ from those seen during training, and task generalization, when a familiar scene is paired with a different language instruction than the one seen in training. A growing family of methods addresses this brittleness by endowing a policy with the spatial and task-aware information such as 2D pixel-coordinate for object localization and placement. However, we find that existing representation through language prompting or visual prompting does not address the limitations; in contrast, exploiting a 3D point-based representation and feeding it directly to the action head leads to substantial improvements-revealing that how the grounding signal is represented and injected into the VLA is the true game changer. Thus, we propose a lightweight, model-agnostic module that represents the grounding signal in 3D, computes its relative displacement to the gripper, and injects the resulting spatial embedding directly into the action head through adaptive layer normalization. The entire module is a two-layer MLP that requires no changes to the VLA backbone or pretraining pipeline. On LIBERO-PRO, our method improves the average success rate of GR00T-N1.6 from 31.2 to 77.5 points under task perturbation and from 28.1 to 60.2 points under position perturbation (gains of 46.3 and 32.1 points). Comparable gains are achieved for $π_{0.5}$ as well, demonstrating that the mechanism is backbone-agnostic. Together, these results support our central finding: given adequate grounding lifted into 3D, injecting it directly into the action head is what unlocks both spatial and task generalization in VLAs-achievable with nothing more than a lightweight module on top of a pretrained backbone.

---

## 论文详细总结（自动生成）

# 论文总结：Direct Action-Head Injection of A Grounded 3D Point Unlocks Spatial and Task Generalization

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：当前视觉-语言-动作（VLA）模型在大规模预训练后，虽然能理解语义指令并在训练场景中表现良好，但在测试时面临两大泛化瓶颈：
  - **空间泛化**（Spatial Generalization）：物体位置与训练时不同，导致策略失效。
  - **任务泛化**（Task Generalization）：同一场景被赋予一条新指令（如换一个目标物体），策略仍执行训练时记忆的轨迹。
- **现有方法局限**：一些工作通过给策略注入显式空间接地信号（如2D像素坐标），但无论是通过语言提示、视觉提示还是链式推理，都未能系统性解决泛化问题。作者发现，**关键不在于是否提供接地信号，而在于如何表示（2D vs 3D）以及如何注入（动作头 vs 其他模块）** 该信号。
- **整体含义**：本文揭示了“将3D点直接注入动作头”是解锁VLA泛化能力的真正变革者，并提供了一种只需极小修改的通用解决方案。

## 2. 方法论

### 核心思想
- 从任意现成视觉接地模块获取2D目标点，利用深度图和相机参数提升为3D坐标，计算与当前夹爪位置的3D相对位移，通过一个两层MLP编码为空间嵌入，**直接注入扩散Transformer动作头的自适应层归一化（AdaLN）**，从而在预测动作时融入几何信息。

### 关键技术细节
1. **2D → 3D提升**：
   - 利用深度图像和相机参数（内参、外参），将2D像素点 \((u,v)\) 反投影到相机坐标系，再转换到机器人基坐标系，得到3D目标位置 \(p_t\)。
2. **相对位移计算**：
   - 从机器人状态获取当前夹爪位置 \(p_g\)，计算：
     \[
     d = p_t - p_g
     \]
3. **空间嵌入编码**：
   - 使用两层MLP（隐藏维度和输出维度均与动作头隐藏维度一致，如1024）对 \(d\) 编码得到 \(z_{\text{spatial}}\)。
4. **注入动作头**：
   - 原DiT动作头中AdaLN仅以时间步嵌入 \(z_{\text{time}}\) 回归缩放/偏移参数 \(\gamma, \beta\)。本文将 \(z_{\text{spatial}}\) 与 \(z_{\text{time}}\) 逐元素相加后输入同一线性层，即：
     \[
     \gamma, \beta = \text{Linear}(z_{\text{time}} + z_{\text{spatial}})
     \]
   - **初始化技巧**：将MLP第二层权重和偏置初始化为零，使得训练开始时 \(z_{\text{spatial}} = 0\)，不破坏预训练的AdaLN行为，逐步学习融合空间信号。
5. **子目标切换**：将拾取-放置任务分解为“伸手抓取”和“运输放置”两个子阶段，通过测量夹爪上目标物体的Z坐标变化（上升超过1cm）自动切换当前子目标对应的目标点（物体→放置区域）。

## 3. 实验设计

### 使用数据集与场景
- **仿真**：LIBERO（LIBERO-Object / LIBERO-Spatial）和 **LIBERO-PRO**（专门用于测试任务扰动和位置扰动的扩展版）。默认训练集包含69个任务（原本LIBERO-Object和Spatial共20个任务 + 从LIBERO-90中选取的49个拾取-放置任务）。
- **真实世界**：Franka Emika Panda机器人 + 两个RealSense D435摄像头（正面RGB-D + 腕装RGB）。收集8个拾取-放置任务，每个任务40条演示。

### Benchmark
- 主要评估在 **LIBERO-PRO** 上的两种扰动：
  - **任务扰动（Task Perturbation）**：场景不变，但指令替换为另一个合理目标物体，测试是否跟随指令而非记忆。
  - **位置扰动（Position Perturbation）**：指令不变，但物体位置改变，测试空间泛化。
- 每个任务执行50次rollout（模拟）或10次rollout（真实），计算平均成功率。

### 对比方法
- **主对比骨干**：GR00T-N1.6 和 π0.5，分别比较有/无本文方法。
- **外部参考模型**：OpenVLA、π0、MolmoAct（推理型模型）。
- **控制消融变体（均使用相同oracle 2D点）**：
  - 2D via Text Prompt：在文本提示中追加2D坐标。
  - 2D via Visual Prompt：在图像上叠加标记。
  - 2D via AdaLN：将2D坐标+2D相对位移通过MLP编码后注入AdaLN。
  - 3D via Text Prompt：在文本中追加3D坐标。
  - 3D via AdaLN (Ours)：本文方法。

## 4. 资源与算力

- 论文中**未明确说明**具体的GPU型号、数量和总训练时长。仅给出了训练配置：
  - GR00T-N1.6：全局batch size 384，训练20,000步；AdamW，lr 1e-4，cosine衰减。
  - π0.5：全局batch size 256，训练30,000步；AdamW，lr 5e-5。
  - 真实世界GR00T-N1.6：batch size 256，训练20,000步。
- 作者未提及硬件资源，因此无法量化算力消耗。

## 5. 实验数量与充分性

### 实验组数（约10组以上）
1. **主结果**（表1）：在LIBERO和LIBERO-PRO上对比两种骨干和三种外部模型，包含In-Distribution、Task Perturbation、Position Perturbation三个子项。
2. **解耦表示与注入**（表2）：在GR00T-N1.6上比较5种变体（加上baseline共6个设置），在两种扰动上分别报告Object/Spatial平均。
3. **数据规模对比**（图3）：将训练集从20个任务扩展到69个任务，对比所有变体。
4. **深度噪声鲁棒性**（图4）：在不同噪声等级（σ=0.01, 0.03）下训练和测试，共3×3=9种条件组合，加上baseline共10种设置。
5. **空间嵌入输入成分消融**（表3）：比较使用 \(p_t\)、\(p_g\)、\(d\) 的不同组合。
6. **注入机制消融**（表4）：对比机器人状态方式 vs AdaLN方式。
7. **真实世界实验**：8个任务，每个任务在三个条件（In-Dist、Task Pert、Pos Pert）下各10次rollout，共240个轨迹；对比baseline和2D Text/Visual Prompt变体。

### 充分性与公平性
- **充分性**：涵盖了多种骨干、多种注入方式、不同数据规模、深度噪声、子目标切换消融，实验较为全面。
- **公平性**：所有控制变体使用**相同的oracle 2D目标点**，排除了接地信号质量差异带来的影响。主实验中外部模型使用各自开源权重，训练数据相同（69任务）。训练超参数遵循官方推荐，仅调整batch size。
- **潜在偏差**：模拟实验中2D目标点来自oracle分割掩码，真实世界中来自Qwen3-VL-4B（无微调），后者可能在不同任务上有性能变化。但论文重点在表示和注入机制，接地源本身不是主要对比对象。

## 6. 主要结论与发现

1. **“表示+注入”双设计决定泛化**：同等oracle 2D点下，2D通过文本或视觉提示仅带来微小提升（<10%），而3D通过AdaLN注入带来巨大提升（>40%）。
2. **3D是关键**：因为动作预测在3D物理空间进行，3D信号匹配输出几何；2D信号迫使VLA内部学习2D→3D映射，难以有效。
3. **直接注入动作头是关键**：即使将3D坐标放入文本提示（经VLM token化）效果也很差，说明几何信息在骨干中流失；通过AdaLN直接注入动作头才能保留。
4. **主干无关**：在GR00T-N1.6和π0.5上均获得大幅改进，证明机制通用。
5. **设计选择优于数据扩展**：本文方法在20个任务上训练的性能（62.0% Task Perturbation）已超过其他所有方法在69个任务上的表现，说明好的结构设计比单纯增加数据重要。
6. **对深度噪声鲁棒**：训练时加入噪声会略有下降但仍优于baseline；测试时对噪声不敏感。

## 7. 优点

- **简洁轻量**：仅需一个两层MLP（约几百万参数），不改变VLA主干结构或预训练流程。
- **模型无关**：可即插即用于任何采用DiT动作头的VLA（如GR00T-N1.6、π0.5）。
- **实验设计严谨**：通过控制变量精确隔离“表示”和“注入”两个维度的影响，避免了混淆。
- **消融全面**：包括输入成分、注入方式、数据规模、深度噪声、真实世界验证，结论可靠。
- **真实世界验证**：使用消费级RGB-D传感器和现成VLM，验证了实用性和噪声下的鲁棒性。
- **子目标切换策略**：简单有效，无需额外检测网络，与整体方法兼容。
- **初始化技巧**：零初始化MLP第二层保证了训练开始时不破坏预训练条件，稳定训练。

## 8. 不足与局限

- **适用范围假设**：
  - 每个子目标必须可关联一个目标物体或区域（如拾取-放置），**不适用于物体无关运动**（如空中挥舞、清扫等）。作者提出可将2D轨迹提升为3D作为扩展方向，但未验证。
  - 需要**充足的2D接地信号**。在复杂场景或模糊指令下，准确接地仍然是开放问题，本文使用的Qwen3-VL-4B在部分任务上（如Task3, Task7）成功率偏低，反映出接地源的不完美。
- **单臂机器人**：所有实验仅在单臂Franka Panda上进行，未验证双臂或人形机器人。
- **深度噪声实验仅在模拟中**：真实世界深度噪声的影响没有专门评估（尽管真实世界用了真实传感器）。
- **未与所有最新方法对比**：虽然对比了OpenVLA、π0、MolmoAct等，但未包含如PointVLA、SpatialVLA等显式使用3D的方法（作者在Related Works中提到了，但未进行定量对比）。
- **子目标切换依赖于高度阈值**：仅通过Z坐标变化判断抓取成功，对于细长物体或特殊放置方式可能不够鲁棒。
- **资源信息缺失**：未提供训练所需的GPU类型、数量和用时，影响可复现性评估。

（完）
