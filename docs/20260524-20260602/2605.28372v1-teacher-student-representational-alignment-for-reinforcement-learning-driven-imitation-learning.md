---
title: Teacher-Student Representational Alignment for Reinforcement Learning-Driven Imitation Learning
title_zh: 用于强化学习驱动的模仿学习的师生表征对齐
authors: "Meraj Mammadov, Pedro Zuidberg Dos Martires, Johannes Andreas Stork"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28372v1"
tags: ["query:robot-rl"]
score: 8.0
evidence: 表征对齐减少RL驱动模仿学习中对RL微调的需求
tldr: 模仿学习从状态强化学习策略中训练时，教师策略常利用学生不可得的特权信息，导致效能差距。本文提出教师-学生表征对齐算法，通过自监督对比学习学习共享嵌入空间，并限制教师梯度更新以防止提取私有信息，从而强制教师策略可模仿。在多个机器人领域实验中，该方法显著缩小了模仿差距，学生性能优于现有基线。此项工作为无需微调的端到端模仿学习提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28372v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 842, \"height\": 838, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 645, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 875, \"height\": 296, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 654, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1814, \"height\": 555, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28372v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 872, \"height\": 253, \"label\": \"Table\"}]"
motivation: 教师策略因使用特权信息产生不可模仿的效能差距，而传统模仿学习后强化学习微调成本高昂。
method: 采用自监督对比学习并行训练共享嵌入空间，同时限制教师编码器的梯度更新以隐藏私有观察信息。
result: 在多个领域测试中，学生策略性能显著高于现有基线，模仿差距大幅降低。
conclusion: 所提算法有效缩小了模仿差距，为处理特权信息的模仿学习提供可训练的端到端方法。
---

## 摘要
从基于状态的强化学习（RL）策略进行模仿学习（IL）是克服机器人领域中复杂和高维观测空间维度灾难的常见方法。本文解决了当教师和学生分别学习时出现的不可约模仿差距，以及教师策略可以自由依赖学生无法从其观测中推断的特权状态信息的问题。我们不是在IL之后通过RL微调来改善较差的学生表现（这通常需要全新的训练设置），而是提出了一种新算法，该算法学习一个共享嵌入空间，隐藏特定于智能体的观测，从而通过构造训练可模仿的教师策略。我们与教师策略并行地使用自监督对比学习训练共享嵌入空间，并通过限制其梯度更新编码器网络来防止其提取私有信息。我们在多个示例领域上进行了评估，并与最先进的基线进行了比较，结果表明我们的算法能够实现更高的学生表现，并显著减少模仿差距。

## Abstract
Imitation learning (IL) from a state-based reinforcement learning (RL) policy is a common approach to overcome the curse of dimensionality in complex and high-dimensional observation spaces prevalent in robotics. This paper addresses the irreducible imitation gap that emerges when teacher and student are learned in isolation, and the teacher policy has the liberty to rely on privileged state information that the student cannot infer from its observations. Instead of improving poor student performance with RL finetuning after IL, which often requires a whole new training setup, we propose a novel algorithm which learns a shared embedding space that hides agent-specific observations and thus trains imitable teacher policies by construction. We train the shared embedding space with self-supervised contrastive learning in parallel to the teacher policy and prevent it from extracting private information by limiting its gradients from updating the encoder networks. We perform evaluations on several example domains and compare to state-of-the-art baselines showing that our algorithm enables higher student performance with substantially reduced imitation gap.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：在强化学习（RL）驱动的模仿学习（IL）框架中，通常先训练一个基于低维状态空间的教师策略，再通过IL将行为“蒸馏”到学生策略。然而，教师策略可以访问对学生不可见的“特权信息”（如精确目标位置、完整地图等），导致学生无法完美模仿，产生不可约的**模仿差距（imitation gap）**。
- **现有方案的局限**：传统的应对方法（如RL微调学生策略）需要额外训练开销且可能不稳定；修改教师奖励函数（如SITT）会引入超参数平衡，破坏原有任务目标。
- **本文目标**：设计一种任务无关、无需修改奖励函数的方法，自动抑制教师对特权信息的依赖，使教师策略**天生可模仿**，从而从源头缩小模仿差距。

### 2. 论文提出的方法论
- **核心思想**：学习一个共享的、低维的嵌入空间，该空间仅保留教师和学生观测共有的信息，屏蔽各自私有的信息。教师策略和随后应用的学生策略都在此共享嵌入空间上运行，从而强制教师只能依赖共享信息。
- **关键技术细节**：
    - **多视角假设**：将教师观测 \(o^T\) 和学生观测 \(o^S\) 视为同一环境状态的两个视图，各自包含公共部分 \(c\) 和私有部分 \(p^T, p^S\)。
    - **对比学习**：使用自监督的InfoNCE损失，将同一时刻的教师与学生嵌入作为正样本对，不同时刻的嵌入作为负样本，训练两个编码器（教师编码器、学生编码器）输出单位向量 \(z^T, z^S\)，使得公共信息被保留，私有信息被丢弃。
    - **对齐损失**：显式最大化成对嵌入的相似度（点积），加速收敛。
    - **稳定损失**：防止嵌入在训练过程中发生剧烈偏移，保证策略网络能够稳定学习。
    - **总损失**：\(L_{\text{embedding}} = L_{\text{contrastive}} + L_{\text{alignment}} + L_{\text{stability}}\)。
    - **训练流程**：两阶段交替进行——阶段一，用教师收集的对齐轨迹数据更新编码器（仅用 \(L_{\text{embedding}}\)）；阶段二，固定编码器，用PPO训练策略网络最大化任务奖励。注意：策略梯度不反向传播到编码器，防止教师提取私有信息。

### 3. 实验设计
- **环境（场景）**：
    - **TunnelVision**：网格世界，教师看到周围8个格子；学生只能看到朝向方向的前3个格子，需频繁转向避免障碍物。
    - **CollectHealth**：3D房间（15m×15m），教师知道所有健康盒的精确位置；学生只能通过有限视野的第一人称图像观察，需不断调整朝向。
- **Baselines**：
    - **BC（行为克隆）**：先单独训练教师，再用收集的轨迹通过监督学习训练学生。
    - **SITT**：联合训练教师和学生，通过KL散度惩罚教师访问学生难以模仿的状态，修改教师奖励函数。
- **评估指标**：成功率和模仿差距（教师成功率 - 学生成功率）。
- **实现细节**：所有方法使用相同网络架构和同等实验步数，每个实验至少重复5个随机种子，报告平均成功率。

### 4. 资源与算力
- **未明确说明**：论文正文未提及使用的GPU型号、数量、训练时长等算力信息。

### 5. 实验数量与充分性
- **实验数量**：在两个环境中测试5种方法（完整Ours、Ours w/o alignment、Ours w/o stability、SITT、BC），共10个实验条件。消融实验覆盖了关键损失项（alignment和stability）。
- **充分性评估**：实验设计较为充分——每个条件使用5个随机种子、报告均值、显示学习曲线（图5）和表格（表I）。对照组选择合理（BC和SITT均为该方向强基线）。消融实验验证了各损失项的有效性。
- **客观/公平性**：代码基于相同架构，步数一致，SITT超参数α经过调优，总体公平。但未与其他扩展方法（如DAgger、RL微调）对比，略有不足。

### 6. 论文的主要结论与发现
- 提出的方法（完整版本）在两个环境中均达到**教师与学生成功率几乎一致**（模仿差距 = 0.00 或 0.01），远低于BC（差距0.81/0.53）和SITT（差距0.19/0.43）。
- 消融实验显示：去掉对齐损失导致CollectHealth性能明显下降；去掉稳定损失导致TunnelVision性能大幅下降。完整模型表现最稳健。
- 结论：通过共享表示学习，无需修改奖励函数即可有效缩小模仿差距，且对超参数不敏感（唯一超参数温度τ可学习）。

### 7. 优点
- **任务无关性**：无需环境先验知识，对比学习自动提取共享信息，即插即用。
- **保留原始奖励函数**：不改变教师目标，避免奖励工程或权衡。
- **端到端可训练**：教师策略和表示学习并行训练，无需额外的RL微调阶段。
- **稳定性机制**：显式添加稳定损失以应对表示漂移，提升策略学习鲁棒性。
- **严格的实验对比**：消融实验清楚展示了各损失项的作用，且结果显著优于基线。

### 8. 不足与局限
- **环境范围有限**：仅在两个相对简单的模拟环境（网格世界和3D房间）中测试，未涉及高维连续控制任务（如机械臂操控、四足运动）或真实机器人场景，泛化性存疑。
- **对比基线覆盖不全**：未与DAgger（在线交互）、RL微调（如Bootstrapping RL）等方法比较，也未评估学生策略RL微调后的性能。
- **计算开销**：对比学习需要收集对齐轨迹和额外编码器，未讨论实际计算成本增加。
- **正负样本定义依赖时间对齐**：仅将同时刻观测作为正样本，在部分领域可能不合理（如状态缓慢变化时相邻帧也可作为正样本），论文未讨论这种设计的局限性。
- **稳定损失中的“旧嵌入”定义**：在训练过程中需要保存旧嵌入，可能增加内存开销和对更新间隔的敏感性。

（完）
