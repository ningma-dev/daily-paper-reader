---
title: "SmoothRL: Online Reinforcement Learning During Asynchronous Execution"
title_zh: SmoothRL：异步执行期间的在线强化学习
authors: "Guang Gao, Yuxuan Nong, Baifu Huang, Jianan Wang"
date: 2026-08-30
pdf: "https://arxiv.org/pdf/2608.29768v1"
tags: ["query:robot-rl"]
score: 9.0
evidence: 在异步执行期间用在线强化学习微调预训练的通用机器人策略，兼顾可靠性与实时性。
tldr: 现实世界部署机器人策略需要兼顾可靠性与实时性，但通用大模型推理延迟高，通常采用异步执行与动作分块来隐藏延迟。然而异步执行与梯度型在线强化学习的结合尚少研究。本文提出SmoothRL，在异步推理循环中微调预训练策略，采用值梯度范式和显式建模异步过程，按帧索引将动作块划分为已提交、执行与丢弃区域，仅对执行区域传播梯度，使优化与异步执行的轨迹分布一致。在需高精度和高动态的真实机器人任务中验证了有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.29768v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1620, \"height\": 817, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1019, \"height\": 716, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1408, \"height\": 1171, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1153, \"height\": 970, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 86, \"height\": 85, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1535, \"height\": 233, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 87, \"height\": 90, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1537, \"height\": 231, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 80, \"height\": 79, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1538, \"height\": 230, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1647, \"height\": 460, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 1163, \"height\": 769, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29768v1/fig-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 1309, \"height\": 1019, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.29768v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1174, \"height\": 743, \"label\": \"Table\"}]"
motivation: 现有方法难以同时满足真实部署所需的精度、鲁棒性与实时性：在线RL可提升性能但异步执行下的策略优化尚未被正确建模。
method: SmoothRL在异步推理中微调策略，基于值梯度更新，并显式按帧划分动作块为已提交/执行/丢弃三区，只对执行区反传梯度。
result: 在需高精度和高动态的真实机器人任务中，SmoothRL实现了稳定且平滑的在线学习，优于未建模异步性的基线。
conclusion: SmoothRL为异步执行场景下的在线策略微调提供了有效框架，弥合了实时控制与RL优化之间的差距。
---

## 摘要
在物理世界中部署机器人策略需要满足两个基本需求：可靠性和平滑的实时执行。然而，部署最先进的通用模型在这两方面都带来了挑战。实现现实世界部署所需的精度和鲁棒性需要样本高效的在线强化学习（RL）来适应预训练模型。同时，机器人基础模型规模的不断扩大导致推理延迟更高。为了在高延迟下满足实时约束，现代系统采用异步推理与动作分块，将策略计算与分块执行重叠，以隐藏延迟并实现平滑控制。尽管它们具有互补作用，但将异步执行与基于梯度的在线强化学习相结合仍未得到充分探索。我们提出了SmoothRL，一个在线RL框架，在异步推理循环内微调预训练策略。SmoothRL遵循价值梯度范式，直接使用动作价值函数关于策略动作的梯度来更新策略参数。为了在异步执行下实现正确的优化，SmoothRL在训练期间显式建模异步推理过程。具体来说，每个生成的动作块按帧索引划分为三个区域：承诺区域，包含先前推理周期承诺的动作；执行区域，包含机器人执行的新生成动作；以及丢弃区域，包含被下一推理周期取代的动作。梯度仅通过执行区域传播，确保策略优化与异步执行引起的轨迹分布一致。我们在需要高精度的真实机器人任务以及需要异步执行的高动态任务上评估了SmoothRL。

## Abstract
Deploying robot policies in the physical world requires satisfying two fundamental desiderata: reliability and smooth real-time execution. However, deploying state-of-the-art generalist models presents challenges on both fronts. Achieving the precision and robustness required for real-world deployment necessitates sample-efficient online reinforcement learning (RL) to adapt pretrained models. Meanwhile, the increasing scale of robot foundation models has led to higher inference latency. To satisfy real-time constraints under high latency, modern systems adopt asynchronous inference with action chunking, overlapping policy computation with chunk execution to hide latency and enable smooth control. Despite their complementary roles, integrating asynchronous execution with gradient-based online RL remains underexplored. We present SmoothRL, an online RL framework that fine-tunes a pretrained policy within an asynchronous inference loop. SmoothRL follows a value-gradient paradigm, directly updating policy parameters using gradients of the action-value function with respect to policy actions. To enable correct optimization under asynchronous execution, SmoothRL explicitly models the asynchronous inference process during training. Specifically, each generated action chunk is partitioned by frame index into three regions: a committed region, consisting of actions committed by the previous inference cycle; an execution region, containing newly generated actions executed by the robot; and a discarded region, containing actions superseded by the next inference cycle. Gradients are propagated only through the execution region, ensuring policy optimization aligns with the trajectory distribution induced by asynchronous execution. We evaluate SmoothRL on real-world robotic tasks requiring high precision, as well as highly dynamic tasks that necessitate asynchronous execution.

---

## 论文详细总结（自动生成）

# SmoothRL论文深度解读

## 1. 核心问题与整体含义

- **背景矛盾**：现实世界的机器人部署必须同时满足两方面的要求——**可靠性**（足够精准、鲁棒地完成任务）与**平滑实时执行**。然而，最先进的通用型机器人基础模型在这两方面都面临挑战。
- **可靠性缺口**：预训练模型面对具体场景时精度与鲁棒性不足，需要**样本高效的在线强化学习（RL）** 进行快速微调，才能适应真实部署环境。
- **实时性瓶颈**：随着模型规模扩大，推理延迟显著上升，难以满足硬实时控制约束。
- **已有折中方案**：现代机器人系统普遍采用**异步推理 + 动作分块（action chunking）**，通过将策略计算与块状执行在时间上重叠来隐藏延迟、维持平滑控制。
- **核心未解问题**：异步执行与基于梯度的在线 RL 各自被广泛研究，但将两者**正确地结合起来**却一直未被深入探索。异步执行会改变状态访问分布与动作-观测对齐方式，若在线 RL 优化过程不显式考虑该差异，训练目标与实际部署轨迹分布会发生错配。

> **本文核心问题**：如何在异步推理循环内部，正确地执行基于梯度的在线策略微调，使优化过程与异步执行所诱导的真实轨迹分布保持一致？

---

## 2. 方法论：SmoothRL

### 2.1 核心思想

SmoothRL 将在线 RL 的优化过程**嵌入到异步推理循环内部**——推理线程在生成动作块供机器人执行的同时，利用执行反馈数据微调预训练策略。优化采用**值梯度范式（value-gradient paradigm）**：不再依赖策略梯度估计器（如 PPO 的似然比），而是直接利用动作-价值函数 \(Q(s,a)\) 对动作的梯度来更新策略参数，即 \(\nabla_\theta J \propto \nabla_a Q(s,a) \cdot \nabla_\theta \pi_\theta(s)\)。这种方式天然适合**确定性策略/低方差更新**，在真实机器人上更为稳定。

### 2.2 关键洞察：动作块的区域划分

在异步执行中，推理与执行各自按不同节奏推进，导致**每个生成的动作块在时间轴上与实际执行产生错位**。SmoothRL 的关键技术在于：训练期间**显式建模异步推理过程**，将每个动作块按帧索引划分为三个区域：

- **承诺区域（Committed Region）**：由上一推理周期已提交并正在执行的动作构成。这些动作**不是当前策略新决策的结果**，不应被本轮更新影响。
- **执行区域（Execution Region）**：当前推理周期**新生成**且机器人实际执行的动作。这是当前策略行为产生真实环境反馈的部分，因此是**唯一的有效学习信号来源**。
- **丢弃区域（Discarded Region）**：在下一推理周期到来前尚未执行、但已被新动作块顶替取代的动作。**它们从未真正作用于环境**，不承载有效的执行后果。

### 2.3 梯度传播规则

- **只对执行区域传播梯度**。承诺区域与丢弃区域的梯度被显式阻断（梯度掩码/停梯度操作）。
- 背后的逻辑是：RL 的训练目标要求每个策略更新的依据必须是**策略确实导致的环境结果**。异步执行下，承诺区域动作产生于旧推理周期的旧策略，丢弃区域动作从未被执行，若将二者纳入优化目标，梯度方向将与异步系统实际运行的状态-动作分布不一致，造成优化偏差。

### 2.4 算法流程示意

1. 预训练策略 \(\pi_\theta\) 加载入异步推理系统。
2. 推理循环按固定周期生成动作块，机器人并行执行。
3. 每步通过公式 \(y = r + \gamma \max_{a'} Q_{\bar{\psi}}(s', a')\) 等自举方式计算目标价值，训练 Critic \(Q_\psi\)。
4. Actor 更新时，沿 \(\nabla_a Q_\psi(s,a)\) 方向、**仅对执行区域的动作**回传 \(\nabla_\theta\pi_\theta(s)\) 的梯度。
5. 重复该过程直至策略收敛或部署完成。

---

## 3. 实验设计

| 维度 | 具体信息 |
|---|---|
| **任务场景** | 两类真实机器人任务：(1) **高精度任务**——需要精细操作的场景；(2) **高动态任务**——机器人运动快、延迟约束紧，必须异步执行才能满足实时控制。 |
| **Benchmark** | 文中未公开标准仿真 benchmark（如 no mention of Robosuite/Isaac 等），而是**以真实机器人任务为评测核心**。 |
| **对比方法** | 论文重点对比的是**未显式建模异步性的基线 RL 方法**，即把异步执行产生的数据当作同步数据直接训练的做法，以验证“显式建模异步过程”是必要的。 |
| **评测指标** | 围绕任务成功率/精度与控制平滑性展开（正文中提供了定性视频/轨迹展示）。 |

> ⚠️ **说明**：提取文本中对任务的具体名称、采样数、评价指标数值、基线方法详细列表等未展开呈现。如需完整实验矩阵，需阅读全文图表部分（图 1-13 与表 1）。

---

## 4. 资源与算力

- 论文提取内容中**未明确报告 GPU 型号、数量、训练时长、机器人平台**等具体算力与实验资源配置信息。
- 需要说明：真实机器人 RL 通常涉及的额外硬件（机械臂、遥操作设备、实时推理端硬件等）也未在文本中给出清单。
- 因此，关于资源与算力部分，本摘要只能指出**文中未披露具体数值**，无法汇总。

---

## 5. 实验数量与充分性

- **实验模块**：主实验覆盖高精度与高动态两类任务；存在对比实验（SmoothRL vs. 未建模异步性的基线优化）；存在可视化/轨迹细节展示（图 1-13 中的学习曲线、动作块区域划分示意等）。
- **充分性评估**：
  - 正面：选题聚焦且任务差异化明显（高精度@低动态 vs. 高延迟@高动态），能够覆盖核心主张的两个维度；在真实物理平台上验证，外部效度较好。
  - 不足：未见大规模消融（如不同异步步长、不同分块长度、区域划分策略敏感性）的详细数据；在提取文本中未见统计显著性说明、多次重复实验的方差报告。
  - 公平性风险：若对比基线为“未建模异步性”的朴素在线 RL，则验证的是**方法本身的设计动机**，这合理但单薄——相较于其他在线 RL 范式（如 off-policy Q-learning、SAC、PPO 适配异步）的系统性比较尚未展示全貌（需看全文确认）。

---

## 6. 主要结论与发现

- **异步执行与在线 RL 可以打通**：SmoothRL 证明了在异步推理循环内直接微调预训练策略是可行的，不再需要将 RL 阶段与异步执行阶段分离。
- **必须显式建模异步过程才能正确优化**：将动作块分三区并**只对执行区域传播梯度**，是保证优化目标与实际轨迹分布一致的关键。
- **可靠性与实时性可以兼得**：在需要高精度的任务和需要异步执行的高动态任务上，SmoothRL 均实现了**稳定且平滑**的在线学习，性能优于未建模异步性的基线。
- **为部署大模型机器人策略提供了新路径**：SmoothRL 弥合了实时控制与基于梯度的 RL 优化之间的差距，是向“在真实机器人上、在线适应大规模策略”迈出的实质性一步。

---

## 7. 亮点与优势

- **问题定位精准**：识别出异步执行+动作分块与梯度型在线 RL 之间长期被忽略的错配问题，填补了真实部署环节中的重要空白。
- **方法简洁优雅**：三区划分（承诺/执行/丢弃）+ 梯度掩码的概念直观、工程实现成本低，不依赖对底层 RL 算法的根本性重构。
- **理论正确性优先**：不是简单地把 RL 勉强塞进异步循环，而是从“梯度必须对应真实执行后果”的第一性原理出发重构优化目标，因果逻辑清晰。
- **真实机器人验证**：在高精度与高动态两类真实物理任务上验证，避免仅在仿真中自说自话，实践说服力强。
- **与现有系统兼容性好**：可直接对接异步推理与动作分块的主流部署架构，无需改变推理管线，迁移成本低。

---

## 8. 不足与局限

- **实验细节披露不完整**：从摘要与元数据中无法获得具体任务名称、成功率数值、学习曲线细节等——需要查看完整正文后才能判断实际效果幅度。
- **算力与资源信息缺失**：未报告 GPU/训练时长等，使复现成本难以评估。
- **消融不足的隐忧**：对“异步延迟程度、动作分块长度、执行区域宽度”等核心超参数如何影响性能的问题，提取文本中未见充分讨论；这些是异步 RL 最关键的敏感性因素。
- **基线对照范围可能偏窄**：如仅与“未建模异步性的变体”对比，则偏重于展示设计必要性；与 SAC/DDPG 等标准 off-policy 算法的系统公平对比有待确认。
- **策略类别假设**：值梯度更新通常天然适配确定性/连续控制策略。方法是**否适用于随机策略或离散动作空间**大模型，文中未展开讨论。
- **安全性与长期稳定性**：真实机器人上在线训练涉及安全探索边界、奖励塑形代价、策略退化风险等现实问题，提取文本中对安全机制无描述。
- **泛化范围有限**：验证集中于任务级微调，对跨任务、跨场景迁移时的异步适应能力未予论述。

---

（完）
