---
title: "Knowing When to Stop: Adaptive Action Chunking via Internal Cross-Attention Dynamics in VLAs"
title_zh: 知晓何时停止：基于内部交叉注意力动力学的自适应动作分块在视觉-语言-动作模型中的应用
authors: "Runze Xu, Xiaolong Shan, Shuang Dai, Yu Wang, Jincheng Yu"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.00908v2"
tags: ["query:vla-policy"]
score: 9.0
evidence: 基于VLA内部交叉注意力熵提出自适应动作分块，优化机器人动作执行
tldr: 固定执行步长的VLA在效率和准确性之间存在权衡，短步长频繁推理易震荡，长步长易与新状态失配。本文利用动作专家中动作到观测的交叉注意力动态，发现预测步长增加时注意力分散且熵趋平台，此信号可指示当前观测对开环执行支撑不足。据此提出免训练的截断机制，检测持续高熵平台并动态决定执行步长，在保留闭环控制的同时提升任务成功率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.00908v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 900, \"height\": 276, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.00908v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1842, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.00908v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1854, \"height\": 768, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.00908v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1627, \"height\": 363, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.00908v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 887, \"height\": 426, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.00908v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 897, \"height\": 474, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.00908v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 904, \"height\": 461, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.00908v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1844, \"height\": 571, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.00908v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 907, \"height\": 569, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.00908v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 906, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.00908v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 905, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.00908v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 843, \"height\": 138, \"label\": \"Table\"}]"
motivation: 固定动作块长度无法兼顾效率与准确，长块易与最新观察错位，短块易振荡，需根据内部状态自适应决定执行长度。
method: 利用动作专家内部交叉注意力熵的上升与平台期作为在线信号，提出免训练的动态截断机制，在推理中自动选择执行步长。
result: 在Pi0.5、X-VLA及RoboTwin/LIBERO/真机上，较固定步长和自适应基线提升平均成功率且开销可忽略。
conclusion: 交叉注意力动态可作为VLA自适应动作执行的实用内部信号，兼顾精度与闭环效率。
---

## 摘要
动作分块是现代视觉-语言-动作（VLA）框架中的标准执行策略，但固定的执行范围在效率和准确性之间引入了权衡。短分块需要频繁推理，且可能导致振荡行为；而长分块则可能与新观察到的状态不一致。我们通过一种基于动作专家内部交叉注意力动力学的自适应动作分块方法来解决这一局限。我们观察到，随着预测范围延长，动作到观察的交叉注意力变得越来越分散，其熵向平台期上升。这一模式与更高的动作预测误差相关，并提供了一个在线信号，表明当前观察对进一步开环执行的依据有限。基于这一观察，我们引入了一种无需训练的截断机制，该机制检测持续的高熵平台，并在推理过程中动态选择执行范围。该方法利用策略已计算的注意力权重，且引入的额外开销可忽略不计。在RoboTwin 2.0、LIBERO以及三项真实世界操作任务上对π_{0.5}和X-VLA的评估表明，相比固定范围和自适应分块基线，平均任务成功率有所提高，同时保持了高效的闭环控制。这些结果表明，交叉注意力动力学可以为VLA中的自适应动作执行提供实用的内部信号。

## Abstract
Action chunking is a standard execution strategy in modern Vision-Language-Action (VLA) frameworks, but fixed execution horizons impose a trade-off between efficiency and accuracy. Short chunks require frequent inference and may cause oscillatory behavior, whereas long chunks can become misaligned with newly observed states. We address this limitation with an adaptive action chunking approach based on internal cross-attention dynamics in the action expert. We observe that, as the prediction horizon extends, action-to-observation cross-attention becomes increasingly dispersed and its entropy rises toward a plateau. This pattern is associated with higher action prediction error and provides an online signal that the current observation offers limited grounding for further open-loop execution. Based on this observation, we introduce a training-free truncation mechanism that detects sustained high-entropy plateaus and dynamically selects the execution horizon during inference. The method uses attention weights already computed by the policy and introduces negligible additional overhead. Evaluations on $π_{0.5}$ and X-VLA across RoboTwin 2.0, LIBERO, and three real-world manipulation tasks show improved average task success over fixed-horizon and adaptive chunking baselines, while preserving efficient closed-loop control. These results show that cross-attention dynamics can provide a practical internal signal for adaptive action execution in VLAs.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：在 Vision-Language-Action（VLA）模型中，动作分块（action chunking）已成为标准执行策略，能有效缓解单步策略中误差累积、时间逻辑缺失等问题。然而，固定分块长度（固定执行步长）存在固有矛盾：
  - **短分块**：推理频繁、计算开销大，且易引发振荡行为；
  - **长分块**：预测的动作与实时环境状态脱节，随预测时间延长，动作精度显著下降。
  - 此外，最优执行长度在不同任务以及同一任务的不同阶段（如快速接近 vs. 精细操作）差异巨大，人工设定固定长度难以泛化。

- **核心含义**：论文提出一种**无需训练的自适应动作分块机制**，利用 VLA 动作专家内部“动作查询到 VLM 观测标记”的交叉注意力熵（cross-attention entropy）作为在线信号，动态判断当前观测对后续开环动作执行的支撑能力，从而自适应决定执行步长，兼顾任务成功率与闭环控制效率。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心观察与假设

- 作者发现，在动作专家生成的预测动作序列中，**动作索引越靠后**，动作到观测的交叉注意力分布越分散；
- 注意力熵随动作索引单调上升，最终进入一个**高熵平台区**（sustained high-entropy plateau）；
- 该高熵平台与**更高的动作预测误差**显著相关——处于最高熵四分位的样本，其动作 MSE 是其余样本的 **10.4 倍**，MSE 标准差为 **12.1 倍**；
- 因此，交叉注意力熵可作为“预测是否足够锚定于当前观测”的**经验性风险信号**。

### 2.2 技术细节

1. **交叉注意力熵计算**：
   - 对于每个动作查询 token \(a_j\)，提取其在各层对 VLM token 的注意力权重 \( \alpha_{l,j,i} \)；
   - 将所有层权重求和并归一化得到 \( p_{j,i} \)：
   \[
   p_{j,i} = \text{Norm}\left(\sum_{l=1}^L \alpha_{l,j,i}\right)
   \]
   - 计算熵：
   \[
   E_j = - \sum_{i=1}^N p_{j,i} \ln p_{j,i}
   \]
   - 熵值上界为 \(\ln N\)（\(N\) 为 VLM token 数），便于归一化比较。

2. **平台检测算法**：
   - 对熵序列 \(E_1, \dots, E_{H_p}\) 进行**移动平均平滑**（窗口 \(k\)）；
   - 计算平滑后的一阶差分（熵梯度）；
   - 判定为“持续高熵平台”需同时满足两个条件：
     - 窗口内所有平滑熵均高于 \(\eta \ln N\)（高熵阈值，论文取 \(\eta = 0.95\)）；
     - 窗口内的平均绝对熵变化小于 \(\tau\)（稳定阈值，取 0.01）。
   - 若检测到平台，取**最早的平台位置 + k** 作为执行步长 \(H_e\)；若未检测到，则执行完整预测长度 \(H_p\)。

3. **训练无关性**：
   - 方法直接复用推理过程中已计算的注意力权重，无需重新训练、无需额外采样、无需修改网络结构。

## 3. 实验设计：数据集 / 场景 / 对比方法

### 3.1 模型与基准

| 模型 | 架构特点 | 预测长度 \(H_p\) |
|------|---------|----------------|
| π0.5 | 解耦式：VLM tokens 作为固定键值，动作查询做交叉注意力 | 50 |
| X-VLA | 耦合式：VLM 与动作 token 拼接后联合自注意力 | 30 |

### 3.2 仿真环境

- **RoboTwin 2.0**：10 个双机械臂操作任务，按平均轨迹长度分为短、中、长三档。π0.5 基于每任务 50 个演示进行微调（40k steps）；X-VLA 使用官方在全部 50 任务上训练的开源 checkpoint。每组配置 3 个随机种子，各 100 次 rollout。
- **LIBERO**：4 个任务套件（Spatial、Object、Goal、Long），使用公开的 π0.5 LIBERO 预训练 checkpoint，不修改策略参数。

### 3.3 真实世界任务

- 三个任务：**Hang mug**（挂杯子）、**Stack cups**（叠杯子）、**Tidy pens**（整理笔）；
- 平台：ARX R5 双臂从动机器人 + ARX X5 主手遥操作；两只腕部鱼眼相机 + 一只头戴 RGB 相机；图像 20 Hz，底层控制 60 Hz；
- 每个任务：50 条专家演示，单独微调 π0.5（40k steps，batch size 32）；每种执行方法进行 20 次试。

### 3.4 对比方法

- **固定分块长度**：π0.5 用 10/15/20/25；X-VLA 用 15/20/25/30；LIBERO 用 2/4/6/8/10；真实任务用 10/20/30/40/50。
- **自适应基线**：
  - **MS（Multi-Sampling）**：通过对流匹配生成过程多次采样估计动作不确定性（论文 [18]）；
  - **SA（Self-Attention-based）**：利用动作 token 对序列锚点的自注意力权重评价置信度（论文 [19]）。

## 4. 资源与算力

- **论文未提供明确的总体算力信息**（如 GPU 总数、总训练时长、能耗等）。
- 仅提到以下相关信息：
  - CPU/GPU 环境：在 **单张 NVIDIA A100 GPU** 上进行了 π0.5 的延迟分析（非全量训练）；
  - 训练设置：π0.5 微调使用 batch size 32、40,000 步；LIBERO 采用公开预训练 checkpoint，无额外训练；
  - 未报告每个任务训练所需的具体时间或卡时。
- **结论：论文在“资源与算力”方面信息不完整，难以评估整体复现成本。**

## 5. 实验数量与充分性

### 5.1 实验数量总结

| 实验类别 | 平台/场景 | 任务数/套件数 | 种子/重复次数 |
|---------|-----------|---------------|---------------|
| RoboTwin 2.0（π0.5） | 仿真 | 10 任务 | 3 seeds × 100 rollouts |
| RoboTwin 2.0（X-VLA） | 仿真 | 10 任务 | 3 seeds × 100 rollouts |
| LIBERO（π0.5） | 仿真 | 4 套件 | 每个固定长度/方法各 2 次？论文未明说（典型为 20 次），但结果以百分比呈现 |
| 真实世界任务 | 实机 | 3 任务 | 20 trials / 方法 |

### 5.2 充分性评价

- **覆盖面广**：两种架构类型（解耦式与耦合式）、两大仿真基准、多项真实任务，基线包含固定长度和两种自适应方法，整体实验设计较为全面。
- **主要不足**：
  - LIBERO 实验未明确说明每个配置的试次数与重复种子数量；
  - 未进行 **消融实验**（如不同 \(\tau\)、\(\eta\)、窗口 \(k\) 的影响）；仅提到统一超参数，但未展示敏感性分析；
  - 未与传统启发式（如按固定窗口+碰撞检测）或“实时闭环单步”进一步比较；
  - 未在更多 SOTA VLA 模型（如 RDT、GR00T）上验证；
  - 样本量上 RoboTwin 每任务只报告了三次种子的平均成功率，未提供方差或显著性检验；
  - 真实世界任务每任务仅 20 次试验，绝对数量偏少，统计可靠性有限。

## 6. 论文的主要结论与发现

- **交叉注意力熵作为内部信号有效**：在 π0.5 和 X-VLA 中均可观测到“晚逐步动作的注意力熵上升—平台”现象，且与动作预测误差（MSE）显著正相关（皮尔逊 \(r=0.187\)，斯皮尔曼 \(\rho=0.432\)，偏相关 \(r=0.214\)，均 \(p<10^{-120}\)）；
- **自适应截断优于固定分块**：
  - RoboTwin π0.5：平均成功率 **62.9%**，明显优于所有固定块长（最佳固定块长为 10，仅 61.6%），也优于 SA（57.6%）和 MS（57.0%）；
  - RoboTwin X-VLA：平均成功率 **79.0%**，超过所有固定长度（最优为块长 30 的 76.1%）；
  - LIBERO：平均成功率 **97.25%**，优于最佳固定块长 6 的 94.88%，尤其在 LIBERO-Long 上提升 4.5 个百分点；
  - 真实世界：平均成功率 **61.7%**，比最佳固定块长（块长 30 的 51.7%）高 10 个百分点，比 SA 基线（55%）高 6.7 个百分点。
- **开销可忽略**：注意力截断方法每轮推理延迟为 0.2725 s，仅比固定分块多约 6–7 ms，远低于多采样（MS）方法的 29.5 ms 增量。
- **任务阶段自适应</b>**：粗放接近阶段熵上升平缓，策略可保持较长的执行步长；精细交互前熵快速达到平台，系统会提前终止开环执行、重新决策，从而避免无效动作。

## 7. 优点

1. **新颖且简洁**：第一个系统地利用“动作到 VLM 的交叉注意力熵”作为 VLA 在线置信度信号，思路直观，无需额外训练或架构改造。
2. **通用性强**：同时适用于解耦式（π0.5）和耦合式（X-VLA）两类主流 VLA 架构，仅需提取相应的注意力子矩阵；统一超参数（\(k=5, \tau=0.01, \eta=0.95\)）在不同基准和任务间保持一致，泛化性较好。
3. **推理开销极低**：复用已有注意力权重，延迟增量 <7 ms，不会破坏实时闭环控制。
4. **实验维度丰富**：涵盖仿真大规模基准和真实机器人任务，并对比固定长度、多采样、自注意力基线，验证充分。
5. **可解释性**：将注意力熵与误差的非线性关系（最高熵区间误差剧增）进行量化分析，提供了对“何时该停止开环执行”的经验性机理解释。

## 8. 不足与局限

1. **依赖交叉注意力存在性**：若架构没有可辨识的 action-to-VLM 交叉注意力（如深层耦合、全注意力或 RNN 结构），该方法无法直接应用——论文自己也承认此类局限，未评估 World Action Model（WAM）等新兴范式。
2. **未展示超参数的敏感性分析**：虽然设置了统一超参数，但没有展示 \(k\)、\(\eta\)、\(\tau\) 的扰动对成功率的影响，稳定性证据不足。
3. **统计严谨性欠佳**：
   - 多数结果只报告了平均成功率，缺少方差、置信区间和显著性检验；
   - 真实任务试次数较少，可能有偶然性。
4. **LIBERO 实验细节不透明**：未说明 rollout 次数、随机种子数量，无法完全评估其在简单基准中接近饱和时的区分能力。
5. **对“熵作为不确定性”的解释性关联而非因果**：作者展示了熵与误差的相关性，但未证明是“熵高导致误差高”或存在第三因素（如动作幅度大）导致两者同时增强；虽然做了部分相关控制，但未讨论反向因果。
6. **未与其他动态执行策略充分对比**：未与基于奖励/结果反馈的在线终止（如早停）等方法对比。
7. **资源信息缺失**：并未提供完整的训练算力与环境配置信息，不利于复现与公平比较。
8. **时效性注意**：论文标注为 2026 年 arXiv 预印本，部分引用文献版本（如 π0.5、X-VLA）仅以 arXiv/会议形式存在，未经过严格同行评审；结论尚需要更多外部验证。

（完）
