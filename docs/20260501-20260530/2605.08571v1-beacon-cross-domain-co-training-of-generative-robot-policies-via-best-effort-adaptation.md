---
title: "BEACON: Cross-Domain Co-Training of Generative Robot Policies via Best-Effort Adaptation"
title_zh: BEACON：基于尽力适配的生成式机器人策略跨域协同训练
authors: "Antong Zhang, Han Qi, Heng Yang"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08571v1"
tags: ["query:vla-policy"]
score: 9.0
evidence: 生成式机器人策略的跨域共同训练
tldr: 机器人策略跨域迁移面临源域与目标域分布差异且目标域演示稀缺的挑战。本文提出BEACON框架，将跨域协同训练建模为差异感知的重要性重加权问题，联合学习扩散策略和样本权重，并开发可扩展的交替优化算法。在sim-to-sim、sim-to-real和多源场景下，BEACON相比基线方法显著提升鲁棒性与数据效率。该框架无需显式对齐即可隐式实现特征对齐，为生成式机器人策略的实用化训练提供了理论指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08571v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1410, \"height\": 694, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.08571v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1128, \"height\": 823, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.08571v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1442, \"height\": 628, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.08571v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 631, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.08571v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1122, \"height\": 698, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.08571v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1444, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.08571v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1443, \"height\": 465, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.08571v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1457, \"height\": 439, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.08571v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1457, \"height\": 405, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.08571v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1458, \"height\": 212, \"label\": \"Table\"}]"
motivation: 现有方法在目标域数据稀缺时难以有效利用源域知识，跨域分布差异导致策略泛化性能下降。
method: 将跨域协同训练转化为差异感知的重要性重加权问题，联合学习扩散策略与源样本权重，并采用随机交替更新优化。
result: 在sim-to-sim、sim-to-real及多源操作场景中，BEACON相比基线提升鲁棒性和数据效率，并隐式实现特征对齐。
conclusion: BEACON为跨域机器人策略训练提供了理论驱动且实用的协同训练框架，无需显式对齐即可有效泛化。
---

## 摘要
我们提出了BEACON——跨域协同训练的尽力适配方法——一个理论驱动的框架，用于利用丰富的源域演示和有限的目标域演示来训练生成式机器人策略。BEACON将跨域协同训练视为一个感知差异的重要性重加权问题，联合学习一个基于扩散的视觉运动策略和每个样本的源权重，以最小化由目标域泛化保证指导的目标。为了使尽力适配在高维序列策略中实用，我们开发了可扩展的实例级差异估计器、策略和权重的随机交替更新，以及平衡异构源域的多源扩展。在仿真到仿真、仿真到真实以及多源操作设置中，BEACON在稳健性和数据效率上优于仅目标域、固定比例协同训练和特征对齐基线。重要的是，即使没有显式的对齐目标，BEACON也通过差异感知的跨域协同训练隐式实现了特征对齐。

## Abstract
We introduce BEACON--Best-Effort Adaptation for Cross-Domain Co-Training--a theory-driven framework for training generative robot policies with abundant source demonstrations and limited target demonstrations. BEACON casts cross-domain co-training as a discrepancy-aware importance-reweighting problem, jointly learning a diffusion-based visuomotor policy and per-sample source weights that minimize an objective informed by target-domain generalization guarantees. To make best-effort adaptation practical for high-dimensional sequence policies, we develop scalable instance-level discrepancy estimators, stochastic alternating updates for policy and weights, and a multi-source extension that balances heterogeneous source domains. Across sim-to-sim, sim-to-real, and multi-source manipulation settings, BEACON improves robustness and data efficiency over target-only, fixed-ratio co-training, and feature-alignment baselines. Importantly, even without an explicit alignment objective, BEACON achieves feature alignment as an implicit result of discrepancy-aware cross-domain co-training.

---

## 论文详细总结（自动生成）

# BEACON 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：机器人行为克隆（BC）中，如何在拥有大量源域（如仿真）演示但目标域（如真实世界）演示极其稀缺的情况下，有效利用源域数据训练泛化性能强的策略。源域与目标域之间存在视觉（纹理、光照、视角）和物理（接触、摩擦、执行延迟）等多方面的分布差异，直接混合训练会导致性能下降。
- **研究动机**：现有方法如域随机化、域适应、特征对齐或固定比例协同训练，要么没有直接优化目标域泛化保证，要么需要选择特定层进行对齐，难以处理非视觉差异和多源场景。
- **整体含义**：BEACON提出一种理论驱动的框架，将跨域协同训练转化为**差异感知的重要性重加权**问题，通过直接最小化基于泛化界的损失函数来联合学习策略和样本权重，从而在不依赖显式对齐的条件下提升目标域性能。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：借鉴Best-Effort Adaptation (BEA)理论，为每个训练样本（源域和目标域）学习一个权重 \(q_i\)，使得加权后的经验风险能逼近目标域的泛化误差。目标函数包含：加权行为克隆损失、源域差异惩罚项、权重与目标参考分布的偏差项以及复杂度正则项。
- **关键技术细节**：
  - **实例级差异估计**：默认使用k-NN在策略视觉编码器嵌入空间中计算每个源样本与最近邻目标样本的距离比，作为该样本的差异分数 \(d_i\)。该估计器随训练周期更新，允许权重与表征协同演化。
  - **随机交替优化**：在一个epoch内交替更新权重和策略参数。权重更新采用投影次梯度下降，考虑损失、差异、正则项和箱约束；策略更新使用AdamW优化加权后的扩散行为克隆损失。
  - **多源扩展（MS-BEACON）**：引入两层权重结构——域级权重 \(w\)（位于概率单纯形上）和样本级权重 \(\tilde{q}\)，通过交替优化 \(w\)、\(\tilde{q}\) 和策略参数，实现异构源域的自动平衡。
- **公式与算法流程（文字说明）**：
  - 优化目标（公式6）：
    \[
    \min_{\theta, q} \sum_i q_i (\ell_{\text{BC}}(\theta; x_i, y_i) + \lambda_d d_i) + \gamma \|q\|_{\infty} R(\theta) + \lambda_1 \|q - p_0\|_1 + \lambda_2 \|q\|_2^2
    \]
    其中 \(p_0\) 是仅目标域的参考权重，\(R(\theta)\) 是权重衰减正则项。
  - 权重更新子梯度（公式7）：
    \[
    g_i = \ell_i(\theta) + \lambda_d d_i + \gamma R(\theta) \frac{\mathbb{1}[q_i = \|q\|_{\infty}]}{|\arg\max_j q_j|} + \lambda_1 \text{sign}(q_i - p_{0,i}) + 2\lambda_2 q_i
    \]
    随后进行裁剪和投影满足箱约束和总预算约束。
  - 算法A1和A2详细描述了单源和多源训练循环，包括周期性刷新 \(d_i\)、权重投影、策略更新步骤。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **机器人平台**：Franka Emika Panda机械臂。
- **任务**（三个精度递增的任务）：
  - **Block Stacking**：堆叠方块，有严格（稳定5帧/最后一帧）和宽松（任意时刻）两个成功标准。
  - **Mug Cleanup**：打开抽屉、放入杯子并关闭抽屉。
  - **Threading**：将细杆插入小孔，需要子厘米级对齐。
- **场景与数据量**：
  - **Sim-to-Sim**：源域为robosuite默认仿真，目标域有两种视觉偏移：纹理变化（T）和纹理+视角变化（T+V）。源演示 \(m=500\)，目标演示 \(n=20\)。
  - **Sim-to-Real**：真实世界机器人数据，同时包含视觉和物理差异。源演示 \(m=500\)，目标演示 \(n=40\)。额外进行**OOD评估**（物体位置超出目标演示区域）。
  - **多源**：两个不同的仿真源域（默认+纹理变化），各占50%。
- **对比方法**（所有方法共享相同的Diffusion Policy骨干和编码器）：
  - Target-Only：仅使用目标域数据。
  - Co-Training：固定比例混合源和目标数据。
  - MMD：协同训练中加入最大均值差异特征对齐损失。
  - UOT：使用非平衡最优传输进行时间对齐的特征对齐。
- **评估**：每个任务随机采样初始状态（sim-to-sim: 100次，sim-to-real: 20次），使用成功率指标（宽松与严格）。

## 4. 资源与算力

- **硬件**：单块 **NVIDIA L40S Tensor Core GPU**（48 GB VRAM）。
- **训练时长**：典型一次BEACON完整训练在 **24小时以内** 完成。
- **说明**：论文未给出所有实验的总计算量或GPU数量，但指出每个实验仅需一张GPU即可重现。

## 5. 实验数量与充分性

- **实验组数丰富**：
  - 主实验：sim-to-sim两个gap、三个任务（共6个子实验）；sim-to-real两个任务（Block Stacking和Mug Cleanup，含OOD评估）；多源实验（表2中MS-BEACON）。
  - 消融实验（附录C）：组合两种q更新策略（全凸内点求解 vs. 随机更新）与三种差异估计器（局部标记差异、k-NN、分类器），共6个变体，在Block Stacking T+V任务上对比。
  - 超参数敏感性分析（附录D）：对 \(\lambda_1, \lambda_2, \lambda_d, \alpha\) 进行单变量扫描，并给出sim-to-sim和sim-to-real下的推荐值。
  - 特征可视化（图3、A2）：UMAP展示隐层特征对齐。
  - 权重演化分析（附录F）：展示了训练过程中权重分布变化。
  - 目标数据量缩放实验（图4）：从20到80个目标演示观察性能趋势。
- **充分性与公平性**：
  - 对比方法均采用相同骨干网络和目标数据，仅在源数据利用方式上不同。
  - 实验覆盖了仿真到仿真、仿真到真实、多源、OOD等关键场景，消融和敏感性分析增强了结论的可靠性。
  - **潜在不足**：Sim-to-Real仅测试了Block Stacking和Mug Cleanup，Threading未进行真实实验；OOD评估只针对物体位置偏移；所有实验在单一机器人平台（Franka Panda）上完成。

## 6. 论文的主要结论与发现

- BEACON在所有场景中**一致优于**Target-Only、Co-Training、MMD和UOT基线，在sim-to-sim中平均成功率比最强基线（MMD）高约4-10个百分点，在sim-to-real中提升更显著。
- **隐式特征对齐**：BEACON无需显式对齐损失，即可在策略编码器空间中实现源-目标特征的自然融合，且避免了粗对齐导致的任务相关结构破坏。
- **多源有益**：MS-BEACON在sim-to-real中进一步提升了性能，尤其是在OOD评估中，表明异构源域配合权重控制可以带来更鲁棒的泛化。
- **目标数据放大**：增加目标演示数量（20→80）时，BEACON的性能提升幅度大于基线，说明重加权与更多目标数据互补而非替代。
- **OOD泛化**：BEACON在物体位置超出目标演示范围时仍能保持较好的成功率，而MMD等基线退化严重。

## 7. 优点：方法或实验设计上的亮点

- **理论驱动**：直接基于目标域泛化界设计优化目标，具有坚实的理论动机。
- **通用性**：不依赖特定对齐层或损失函数，可处理任何形式的域偏移（视觉、物理、多源）。
- **实用性**：随机交替优化和k-NN差异估计器使得方法可扩展到高维扩散策略，且易于实现。
- **隐式对齐**：避免了显式对齐可能带来的负迁移，自动学习对任务有益的共享表征。
- **多源扩展**：两层权重结构优雅地解决了异构源域平衡问题。
- **实验设计严谨**：包含多种跨域场景、多个任务、严格和宽松成功标准、OOD评估、消融和敏感性分析，增强结论可信度。

## 8. 不足与局限

- **假设局限性**：论文仅考虑准静态交互（如堆叠、放置），未涉及动态或高速任务（如投掷、抓取动态物体），这些场景下域差异可能更大。
- **应用限制**：假设真实数据极其稀缺，但现实场景中可能存在大量异构真实数据（不同机器人、光照、背景），方法尚未验证将此类数据作为源域时的效果。
- **实验覆盖有限**：仅在一个机器人平台（Franka Panda）上评估，未在其他机械臂或灵巧手上测试。Sim-to-Real任务不包含Threading，该任务对精度要求更高，可能更具挑战。
- **特征对齐隐式性**：虽然隐式对齐是优点，但也可能在某些极端分布下不够稳定，论文未探讨其失效边界。
- **可扩展性**：k-NN差异估计器需要计算所有源样本与目标样本的距离，在源数据极大（如上万）时计算开销可能增加，论文未讨论大规模数据下的效率问题。
- **超参数敏感**：虽然给出了推荐值，但sim-to-sim和sim-to-real的最佳参数不同（尤其是 \(\lambda_d\) 和 \(\alpha\)），需要针对具体任务进行调整。

（完）
