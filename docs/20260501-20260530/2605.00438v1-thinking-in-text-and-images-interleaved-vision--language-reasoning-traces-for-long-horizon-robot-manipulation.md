---
title: "Thinking in Text and Images: Interleaved Vision--Language Reasoning Traces for Long-Horizon Robot Manipulation"
title_zh: 文本与图像中的思维：面向长时域机器人操作的交叉视觉-语言推理轨迹
authors: "Jinkun Liu, Haohan Chi, Lingfeng Zhang, Yifan Xie, YuAn Wang, Long Chen, Hangjun Ye, Xiaoshuai Hao, Wenbo Ding"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00438v1"
tags: ["query:vla-policy"]
score: 9.0
evidence: 交错视觉-语言推理用于长时程操作的VLA策略
tldr: "长期机器人操作需要兼顾逻辑连贯性与空间几何约束，现有基于潜变量或单一模态的规划方法难以同时满足。本文提出IVLR框架，引入交错视觉语言推理轨迹（trace），显式交替生成文本子目标和视觉关键帧覆盖全任务。在LIBERO基准上取得95.5%平均成功率，其中长时任务92.4%，消融表明文本与视觉模态缺一不可。该工作通过显式多模态轨迹提高了策略的泛化能力和执行鲁棒性，为长期复杂操作提供新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00438v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1366, \"height\": 700, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00438v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1372, \"height\": 1001, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00438v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1438, \"height\": 598, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00438v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 1076, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.00438v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1451, \"height\": 467, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.00438v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1430, \"height\": 458, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.00438v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1035, \"height\": 435, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.00438v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1444, \"height\": 437, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.00438v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 794, \"height\": 268, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.00438v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1262, \"height\": 336, \"label\": \"Table\"}]"
motivation: 现有方法使用文本链式思维或视觉预测单一模态，无法同时满足长程操作的语义逻辑与几何约束。
method: 提出IVLR框架，由多模态变压器生成交替文本子目标与视觉关键帧的推理轨迹，并缓存作为动作解码器的条件。
result: "在LIBERO基准上平均成功率95.5%，LIBERO-Long达92.4%，SimplerEnv-WidowX达59.4%。消融表明完整交错轨迹比文本或视觉单一模态高出20%以上。"
conclusion: 交错视觉-语言推理轨迹显式融合语义与几何信息，对局部扰动具有一定鲁棒性，但在全局计划过时时性能受限，展示了显式多模态表示的价值。
---

## 摘要
长时域机器人操作需要既逻辑连贯又基于几何约束的计划。现有的视觉-语言-动作策略通常将规划隐藏在潜在状态中，或仅暴露单一模态：纯文本思维链编码了因果顺序但忽略了空间约束，而视觉预测提供了几何线索但往往局限于局部且语义约束不足。我们提出了交叉视觉-语言推理（IVLR），这是一个围绕\trace{}构建的策略框架，\trace{}是一种显式的中间表示，在整个任务范围内交替呈现文本子目标和视觉关键帧。在测试时，单个原生多模态变换器根据初始观测和指令自主生成这种全局的语义-几何轨迹，并将其缓存，然后以轨迹、原始指令和当前观测为条件，驱动闭环动作解码器。由于标准机器人数据集缺乏此类轨迹，我们通过时间分割演示并用视觉语言模型为每个阶段添加字幕来构建伪监督。在长时域操作和视觉分布偏移的模拟基准测试中，\method{}在LIBERO上达到95.5%的平均成功率，包括LIBERO-Long上的92.4%，以及在SimplerEnv-WidowX上总体成功率为59.4%。消融实验表明两种模态都是必要的：没有轨迹时，LIBERO-Long成功率降至37.7%；仅文本和仅视觉轨迹分别达到62.0%和68.4%，而完整的交叉轨迹达到92.4%。执行扰动和轨迹内容掩码的应力测试显示适度退化，表明轨迹能够容忍局部损坏和适度的执行漂移，但在过时或不正确的全局规划下仍然受限。

## Abstract
Long-horizon robotic manipulation requires plans that are both logically coherent and geometrically grounded. Existing Vision-Language-Action policies usually hide planning in latent states or expose only one modality: text-only chain-of-thought encodes causal order but misses spatial constraints, while visual prediction provides geometric cues but often remains local and semantically underconstrained. We introduce Interleaved Vision--Language Reasoning (IVLR), a policy framework built around \trace{}, an explicit intermediate representation that alternates textual subgoals with visual keyframes over the full task horizon. At test time, a single native multimodal transformer self-generates this global semantic-geometric trace from the initial observation and instruction, caches it, and conditions a closed-loop action decoder on the trace, original instruction, and current observation. Because standard robot datasets lack such traces, we construct pseudo-supervision by temporally segmenting demonstrations and captioning each stage with a vision-language model. Across simulated benchmarks for long-horizon manipulation and visual distribution shift, \method{} reaches 95.5\% average success on LIBERO, including 92.4\% on LIBERO-Long, and 59.4\% overall success on SimplerEnv-WidowX. Ablations show that both modalities are necessary: without traces, LIBERO-Long success drops to 37.7\%; text-only and vision-only traces reach 62.0\% and 68.4\%, while the full interleaved trace reaches 92.4\%. Stress tests with execution perturbations and masked trace content show moderate degradation, suggesting that the trace can tolerate local corruption and moderate execution drift, but remains limited under stale or incorrect global plans.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长时域机器人操作需要同时满足**因果逻辑连贯性**（例如任务阶段顺序）和**空间几何约束**（例如末端执行器位姿、物体接触点）。现有的视觉-语言-动作（VLA）策略要么将规划隐藏在潜变量中，要么仅暴露单一模态——文本链式思维（CoT）编码了因果顺序但缺乏空间细节，视觉预测提供了几何线索但语义模糊且容易漂移。这两种方式都无法兼顾任务全过程的语义和几何需求。
- **研究背景**：尽管RT-1/2、OpenVLA等VLA模型在短时域任务中取得进展，但在多阶段、长距离任务中容易陷入时间近视（过早执行后续子目标或丢失任务状态记忆）。现有显式推理方法（如文本CoT、视觉世界模型）单一模态不足以同时满足因果顺序和空间定位。
- **整体含义**：提出**交错视觉-语言推理轨迹（IVLR-Trace）**——一种显式的、多模态的中间表示，覆盖整个任务范围，交替呈现文本子目标和视觉关键帧，使得策略在运行时既可保持语义逻辑又具有几何锚点，从而提升长时域操作的稳定性和成功概率。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：在机器人策略中引入显式的**IVLR-Trace**，其形式为：
  \[
  z = [(c^{(1)}, k^{(1)}), (c^{(2)}, k^{(2)}), \dots, (c^{(N)}, k^{(N)})]
  \]
  其中 \(c^{(i)}\) 为文本子目标（描述因果角色），\(k^{(i)}\) 为视觉关键帧（描述期望的空间状态）。
- **关键技术细节**：
  - **统一多模态架构**：基于Show-o2 1.5B原生多模态变换器，融合文本、视觉（VAE编码）、动作（[ACT] token+MLP）三种模态。
  - **推理与执行两阶段**：
    - **推理阶段（t=0）**：从初始观测 \(o_0\) 和指令 \(I\) 自回归生成完整轨迹。文本子目标通过语言头解码，视觉关键帧通过流匹配（flow matching）头生成。
    - **执行阶段（t>0）**：缓存的轨迹作为上下文，每步与当前观测 \(o_t\)、原始指令 \(I\) 一起输入动作解码器，预测连续动作 \(a_t = \text{MLP}(h_{[ACT]}(o_t, I, z)) + \mu_{act}\)。无需显式阶段指针，注意力机制自动对齐。
  - **训练目标**：
    \[
    L_{\text{total}} = L_{\text{text}} + \lambda L_{\text{flow}} + \gamma L_{\text{action}}
    \]
    - \(L_{\text{text}}\)：文本子目标的交叉熵损失。
    - \(L_{\text{flow}}\)：视觉关键帧的流匹配损失（VE预测噪声到目标latent的向量场）。
    - \(L_{\text{action}}\)：动作的L1损失。
  - **伪轨迹构建**：利用Universal Visual Decomposer (UVD) 对演示视频进行时间分段，选择每段最后一帧作为关键帧；再使用VLM（Qwen3-VL）为每段生成结构化字幕。整个过程无需人工标注，可扩展。

## 3. 实验设计：数据集 / 场景、Benchmark、对比方法

- **数据集与场景**：
  - **LIBERO** [16]：包含四个模拟操作套件，测试空间泛化、物体变化、目标变化、长时域顺序操作（特别强调LIBERO-Long）。
  - **SimplerEnv-WidowX** [14]：模拟环境，包含光照、背景、视角等视觉分布偏移，用于评估视觉鲁棒性。
- **Benchmark**：各套件中的标准成功率（Success Rate）指标。
- **对比方法**（来自主表及引用）：
  - DP (Diffusion Policy) [7]
  - Octo (Base/Small) [18]
  - OpenVLA [11]
  - SpatialVLA [21]
  - CoT-VLA [31]
  - π0-FAST [19]
  - VLA-0 [8]
  - RT-1-X [33]
  - RoboVLMs [17] 等。

## 4. 资源与算力（明确给出）

- **模型规模**：初始化自Show-o2 1.5B（15亿参数）。
- **推理延迟**：单张NVIDIA H20 GPU上生成完整轨迹约需**10秒**；缓存后动作解码频率**10Hz**（含动作分块）。
- **训练资源**：
  - **硬件**：16张NVIDIA H200 GPU。
  - **训练步数与时长**：
    - LIBERO：约40K步，约4小时。
    - SimplerEnv：约60K步，约6小时。
- **优化器与超参数**：AdamW，学习率模块化（全模型1e-5，动作模型1e-4），余弦衰减。

## 5. 实验数量与充分性

- **实验组数**：
  - **主比较实验**：含2个表（LIBERO表2、SimplerEnv表3），覆盖8+种对比方法，每个套件独立结果。
  - **消融实验**（表4）：5种设置（无轨迹、文本仅轨迹、视觉仅轨迹、完整IVLR），在4套件上报告成功率。
  - **应力测试**（表5）：执行扰动（2cm位移）、文本掩码30%、视觉掩码30%，共3种退化条件，报告绝对下降幅度。
  - **定性示例**（附录D）：展示带轨迹/无轨迹的执行轨迹对比及完整轨迹样例。
  - **伪标注质量控制**（附录A）：描述过滤规则与人工抽查。
- **充分性与公平性**：
  - 实验覆盖多个基准、多种基线方法，消融实验揭示两种模态的必要性，鲁棒性测试评估了局部退化下的表现。
  - 基线结果来自原论文或基准报告，IVLR使用自生成轨迹（非oracle伪轨迹），公平。
  - 未报告多种子置信区间（因算力限制），但单一种子下指标趋势显著。
  - **客观评价**：作者主动指出IVLR并非在所有套件上都占优（如π0-FAST在LIBERO-Object上略高），并详细分析失败模式。

## 6. 论文的主要结论与发现

- **主要结论**：显式的全水平交错轨迹（文本+视觉）显著提升长时域VLA控制能力，两种模态互补且缺一不可。
- **关键数据**：
  - LIBERO平均成功率95.5%，其中**LIBERO-Long 92.4%**（对比无轨迹37.7%、仅文本62.0%、仅视觉68.4%）。
  - SimplerEnv-WidowX总体成功率59.4%（对比SpatialVLA 42.7%）。
- **鲁棒性**：对2cm执行扰动和30%掩码退化为中等程度（约3-4%绝对下降），说明轨迹接口在局部损坏下仍可工作，但全局计划过时或场景变化时性能受限。
- **局限性**（论文自述）：
  - 假设起始时静态、全观测环境。
  - 生成轨迹约10秒延迟，不适用于快速变化场景。
  - 未在真实机器人上验证。
  - 对严重遮挡、物体滑落、场景改变后失效。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - **显式多模态表征**：首次提出用“文本子目标+视觉关键帧”构成完整任务故事板，可解释、可消融、可直接监督。
  - **统一原生多模态变压器**：单一模型完成推理与执行，无需外部规划器或手写阶段指针。
  - **可扩展伪标注**：UVD+VLM自动生成训练监督，无需人工注释，降低了数据获取成本。
  - **闭环执行**：尽管一开始生成完整轨迹，但动作解码始终以当前观测为条件，非开环回放。
- **实验设计亮点**：
  - 多基准（LIBERO + SimplerEnv）覆盖不同难度与泛化维度。
  - 消融实验指出“两种模态都必要”并量化互补收益。
  - 应力测试（扰动/掩码）验证了接口的鲁棒性，并揭示了性能退化的边界。
  - 自省式分析：坦诚讨论失败模式与局限性，不回避短板。

## 8. 不足与局限：实验覆盖、偏差风险、应用限制

- **实验覆盖**：
  - 仅在模拟环境（LIBERO、SimplerEnv-WidowX）中评估，缺乏真实机器人实验验证。
  - 未测试动态变化场景（如物体被移动、人干预），只考虑了静态全观测初始条件。
  - 未报告多随机种子下的置信区间，单一种子可能不稳定。
  - 对全局计划错误（如错误阶顺序）未做压力测试（掩码保留顺序，仅为局部缺失）。
- **偏差风险**：
  - 伪标注质量依赖VLM（Qwen3-VL）和UVD分割，可能存在标注噪声，但文中通过过滤和人工抽查减轻偏差。
  - 超参数（学习率、损失平衡系数λ,γ）仅在默认设置下评估，未进行系统搜索。
- **应用限制**：
  - 10秒的初始规划延迟使得该方法难以应用于人机交互频繁或快速变化的实时场景。
  - 对“初始未观测目标”无法生成有效轨迹，需要先探索，超出框架能力。
  - 若场景在执行中发生重大变化（如物体被移除），缓存轨迹可能完全过时导致失败，目前无重规划机制。

（完）
