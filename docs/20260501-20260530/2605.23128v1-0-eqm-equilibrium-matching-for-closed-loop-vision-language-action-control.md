---
title: "$π_0$-EqM: Equilibrium Matching for Closed-Loop Vision-Language-Action Control"
title_zh: $π_0$-EqM：面向闭环视觉-语言-动作控制的均衡匹配
authors: "Huanming Liu, Congsheng Xu, Jianmin Ji, Yao Mu"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23128v1"
tags: ["query:vla-policy"]
score: 9.0
evidence: 用于VLA闭环控制的均衡匹配解码器
tldr: "当前Vision-Language-Action模型常采用流匹配动作解码器，但固定采样步长限制状态依赖计算和跨周期时序复用。本文提出π0-EqM，用均衡匹配解码器替换流匹配专家，保持上游VLA堆栈不变。在300步预算下，RoboTwin 19任务平均成功率从40.4%提升至50.2%，LIBERO-10达87.0%。实验发现任务依赖的非单调残差-成功率关系，即平稳性-可执行性差距，表明推理深度是策略设计的一部分，并引入基于能量的VLA视角，为跨任务、跨本体的组合动作生成提供新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23128v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 891, \"height\": 1033, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23128v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23128v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 855, \"height\": 657, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.23128v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1563, \"height\": 517, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23128v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 679, \"height\": 298, \"label\": \"Table\"}]"
motivation: 解决VLA控制中固定采样步长造成的状态依赖计算不足和时序复用受限问题。
method: 用均衡匹配解码器替换π0中的流匹配专家，上游VLA堆栈保持不变。
result: "300步预算下RoboTwin平均成功率从40.4%升至50.2%，LIBERO-10达87.0%。"
conclusion: 揭示任务依赖的非单调关系（平稳性-可执行性差距），推理深度是策略设计参数，提供基于能量的VLA新视角。
---

## 摘要
当前，视觉-语言-动作（VLA）模型因其在任务泛化方面的巨大潜力，已成为机器人操作中最常用的范式。然而，大多数用于VLA控制的生成式流匹配动作解码器通常采用固定的采样步长，这限制了状态相关的计算和跨控制周期的时间复用。我们提出$π_0$-EqM，它用均衡匹配（EqM）解码器替换$π_0$中的流匹配专家，同时保持上游VLA堆栈不变。在匹配的300步预算下，$π_0$-EqM在19个任务上将RoboTwin的平均成功率从40.4%提升至50.2%，并在LIBERO上保持竞争力，其在LIBERO-10上的提升最为显著（87.0%）。两次阈值扫描揭示了残差与成功率之间存在任务依赖的非单调关系，我们将其称为平稳性-可执行性差距。结果表明，迭代VLA控制中的推理深度是策略设计的一部分，并引入了一种基于能量的VLA视角，这可能为未来跨任务和本体的可组合动作生成研究提供参考。

## Abstract
Currently, Vision-Language-Action (VLA) models have become the most adopted paradigm for robotic manipulation for its great potential for task generalization. While most generative flow-matching action decoders for VLA control are often deployed with fixed sampling horizons, limiting state-dependent compute and temporal reuse across control cycles. We present $π_0$-EqM, which replaces the flow-matching expert in $π_0$ with an Equilibrium Matching (EqM) decoder while leaving the upstream VLA stack unchanged. Under a matched 300-step budget, $π_0$-EqM improves RoboTwin average success from 40.4% to 50.2% across 19 tasks and remains competitive on LIBERO, with its clearest gain on LIBERO-10 (87.0%). Two threshold scans reveal a task-dependent non-monotonic relation between residual and success, which we term the stationarity--executability gap. The results suggest that inference depth in iterative VLA control is part of policy design and introduce an energy-based VLA perspective that may inform future work on composable action generation across tasks and embodiments.

---

## 论文详细总结（自动生成）

# 论文总结：π₀-EqM：面向闭环视觉-语言-动作控制的均衡匹配

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：当前主流的Vision-Language-Action（VLA）模型（如π₀）使用流匹配（flow-matching）动作解码器生成动作块，但这些解码器通常采用**固定采样步长**，无法根据状态动态调整计算量，且难以在连续控制周期之间复用中间迭代结果（即不支持“热启动”）。这限制了闭环控制中的推理效率与性能。
- **整体含义**：作者提出，推理深度本身应被视为策略设计的一部分，而非固定数值过程。通过引入均衡匹配（Equilibrium Matching, EqM）解码器，使动作生成变为**无时间状态的迭代求解**，从而支持自适应停止和跨周期结构热启动，并引入基于能量的VLA视角，为未来跨任务、跨本体的可组合动作生成提供基础。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：用EqM解码器替换π₀中的流匹配专家。EqM学习一个**时间无关的（自治的）条件向量场**，其零点对应目标动作块。推理时通过迭代求解该向量场的零点来生成动作，摆脱了随扩散时间索引的采样步长限制。
- **关键技术细节**：
  - **训练目标**：给定演示动作块A和结构噪声ε，定义插值A_γ = γA + (1-γ)ε，训练向量场f_θ(A_γ; c_t)匹配目标向量v_target = w(γ)(A - ε)，其中w(γ)在γ→1时衰减至0，使数据流形成为稳定平衡点。损失函数为EqM损失：
    \[
    \mathcal{L}_{\text{EqM}}(\theta) = \mathbb{E}_{\gamma,A,\varepsilon}\left[ \| f_\theta(A_\gamma; c_t) - w(\gamma)(A - \varepsilon) \|_2^2 \right]
    \]
  - **推理求解**：从初始点A_t^(0)出发，使用Nesterov加速法（带动量μ和步长η）迭代更新：
    \[
    \tilde{A}_t^{(k)} = A_t^{(k)} + \mu (A_t^{(k)} - A_t^{(k-1)})
    \]
    \[
    A_t^{(k+1)} = \tilde{A}_t^{(k)} - \eta f_\theta(\tilde{A}_t^{(k)}; c_t)
    \]
  - **自适应停止**：定义归一化查找残差 \( r_k = \frac{\| f_\theta(\tilde{A}_t^{(k)}; c_t) \|_2}{\sqrt{H d}} \)，当r_k ≤ τ或达到最大步数时停止。
  - **结构热启动**：利用前后控制周期的时序相关性，将上一周期输出块的一半复制到当前初始化前半部分，后半部分填充随机噪声，使初始点更接近平衡点，减少所需迭代。
- **理论分析**：局部假设下，迭代等价于递减一个光滑势能函数，残差与迭代次数的对数关系（Proposition 1），解释了热启动的有效性。

## 3. 实验设计：数据集、场景、benchmark、对比方法

- **数据集/场景**：
  - **RoboTwin benchmark**：19个任务，每个任务500个演示（100个干净演示+400个随机化演示），共9500个演示。
  - **LIBERO suites**：包含四个子套件：LIBERO-Spatial、LIBERO-Object、LIBERO-Goal、LIBERO-10。
- **Benchmark**：标准机器人操作成功率。
- **对比方法**：原始π₀（使用流匹配解码器），仅替换动作解码器，保持上游VLA堆栈不变，进行公平对比。主实验在匹配的300步推理预算下进行。

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。作者在实验设置部分仅描述了数据集和评估协议，未涉及硬件配置。因此无法给出具体资源消耗。

## 5. 实验数量与充分性

- **实验组数**：
  - 主实验：在RoboTwin 19个任务上对比π₀与π₀-EqM，报告平均成功率。
  - 在LIBERO四个套件上分别报告成功率。
  - 阈值扫描实验：对RoboTwin中的`place_dual_shoes`和`click_bell`两个任务进行残差阈值扫描（τ从0.1到1.2），展示任务依赖的非单调性；另对`click_alarmclock`展示单任务推理轨迹的定性三个阶段。
- **充分性评价**：
  - **优点**：主实验覆盖了多个任务和标准benchmark，对比公平（仅改解码器）。阈值扫描揭示了关键现象（stationarity-executability gap）。
  - **不足**：阈值扫描仅涵盖两个RoboTwin任务，未在全部19个任务或LIBERO上系统扫描；缺少消融实验来分离EqM目标本身、停止规则、热启动各自贡献；未进行跨本体或多任务组合的泛化实验。整体上实验数量合理但深度有限，结论的普适性需更多验证。

## 6. 主要结论与发现

- **性能提升**：在匹配的300步推理预算下，π₀-EqM在RoboTwin 19任务上平均成功率从40.4%提升至50.2%，提升9.8个百分点；在LIBERO-10上从85.2%提升至87.0%，整体LIBERO平均略有提升（94.15%→94.35%）。
- **核心发现**：残差阈值与成功率之间存在**任务依赖的非单调关系**，即“平稳性-可执行性差距”：数值上更收敛（残差更低）的迭代点并不一定对应物理上更好的执行结果。这表明推理深度是影响策略行为的设计变量。
- **理论意义**：提出基于能量的VLA视角，认为时间无关的迭代求解使得test-time计算分配成为可控的系统变量。

## 7. 优点

- **方法创新**：首次将均衡匹配引入VLA解码器，实现了自适应停止和热启动，打破了扩散/流匹配解码器固定采样步长的限制，更具灵活性。
- **实验设计公平性**：仅替换动作解码器，保持上游VLA栈不变，直接归因于解码器的改变。
- **理论支撑**：通过局部势能下降分析（Proposition 1）为残差阈值提供了数值合理性，并解释了热启动减少迭代的原理。
- **现象发现**：揭示“平稳性-可执行性差距”这一非直观现象，对理解和设计迭代式VLA控制策略有重要指导意义。
- **多benchmark验证**：在RoboTwin（多样任务）和LIBERO（经典套件）两个基准上验证，增强了结论可信度。

## 8. 不足与局限

- **实验覆盖有限**：阈值扫描仅涵盖2个RoboTwin任务，未在全部任务或LIBERO上验证非单调性的普遍性；未进行跨本体或多任务组合实验。
- **消融分析缺失**：未分离EqM目标、停止规则、热启动各自对性能的贡献，无法确定改进来源的主次。
- **理论局限性**：Proposition 1是局部假设（向量场为梯度场），实际学习的场可能非保守，全局收敛性未证明。
- **应用限制**：当前仅验证了单步闭环控制场景；能量可组合性（多任务/多本体）仅为方向性展望，无具体实验。
- **资源信息缺失**：未报告训练/推理算力，影响可复现性和效率比较。

（完）
