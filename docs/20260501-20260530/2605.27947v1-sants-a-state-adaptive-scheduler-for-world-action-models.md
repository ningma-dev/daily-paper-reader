---
title: "SANTS: A State-Adaptive Scheduler for World Action Models"
title_zh: SANTS：面向世界动作模型的状态自适应调度器
authors: "Yirui Sun, Guangyu Zhuge, Keliang Liu, Jie Gu, Xinyu Bing, Zhongxue Gan, Chunxu Tian"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27947v1"
tags: ["query:world-models"]
score: 9.0
evidence: 世界动作模型调度器
tldr: "像素空间世界动作模型中，完全去噪视频并非总是最优，视频细化可能饱和甚至降低动作质量。本文提出状态自适应噪声轨迹调度器SANTS，通过读取视频状态和噪声水平预测停止时机与噪声比例，并用下游动作质量奖励进行后训练。在RoboTwin 2.0和真实机器人任务中分别取得94.4%和73.1%成功率，同时将延迟降低约80%。SANTS在保持未来推理优势下，大幅削减了冗余计算。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27947v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1404, \"height\": 658, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27947v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1274, \"height\": 741, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27947v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1433, \"height\": 552, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27947v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1401, \"height\": 767, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27947v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 869, \"height\": 403, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.27947v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1356, \"height\": 337, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27947v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1133, \"height\": 495, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27947v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1184, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27947v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 643, \"height\": 171, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27947v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 539, \"height\": 456, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27947v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1071, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27947v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1109, \"height\": 572, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27947v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1441, \"height\": 314, \"label\": \"Table\"}]"
motivation: 现有WAM固定使用完全去噪视频作为动作条件，忽略了状态依赖的最优去噪深度，导致推理冗余和潜在的动作次优。
method: SANTS作为轻量级调度器，在每步联合预测累积停止危险和相对噪声进展比，并通过下游动作奖励进行路径级后训练以优化动作质量。
result: "在RoboTwin 2.0上达到94.4%总体成功率，在7个真实机器人任务上平均成功率73.1%，延迟相比全去噪降低81.7%和79.0%。"
conclusion: 自适应噪声轨迹采样能够保留WAM的推理优势，同时消除大部分冗余推理成本，提升效率与性能。
---

## 摘要
世界动作模型通过使用基于视频的未来表示来调节动作生成，从而改进机器人操作。然而，在像素空间的世界动作模型中，最佳动作条件并不一定是完全去噪的视频。受控去噪深度扫描表明，视频细化可以降低动作误差，但存在一个状态依赖的临界点，之后当后期预测与动作相关性降低或物理上不可靠时，增益可能饱和甚至反转。这表明动作生成应沿着视频噪声轨迹使用状态依赖的点，而非固定的终端去噪深度。我们提出了状态自适应噪声轨迹调度器（SANTS），一种用于视频到动作扩散策略的轻量级调度器。在每个视频决策点，SANTS读取当前视频状态表示和噪声水平，然后联合预测累计停止风险与相对噪声递进比率。SANTS通过冻结动作分支生成最终动作块后计算的路径级奖励进行后训练，因此调度器针对下游动作质量而非中间视频保真度进行优化，同时显式惩罚冗余的视频状态更新。实验表明，SANTS在RoboTwin 2.0上达到94.4%的整体成功率，在七个真实机器人任务上平均成功率为73.1%，同时与完整视频去噪相比，延迟分别降低了81.7%和79.0%。这些结果表明，沿视频噪声轨迹的自适应选择可以保留世界动作模型式未来推理的控制优势，同时消除其大量冗余推理成本。

## Abstract
World Action Models (WAMs) improve robot manipulation by using video-based future representations to condition action generation. In pixel-space WAMs, however, the best action condition is not necessarily the fully denoised video. Controlled denoising-depth scans show that video refinement can reduce action error up to a state-dependent point, after which the gain may saturate or even reverse when late predictions become less action-relevant or physically unreliable. This suggests that action generation should use a state-dependent point along the video noise trajectory rather than a fixed terminal denoising depth. We introduce State-Adaptive Noise Trajectory Scheduler (SANTS), a lightweight scheduler for video-to-action diffusion policies. At each video decision point, SANTS reads the current video-state representation and noise level, then jointly predicts a cumulative stopping hazard and a relative noise-progression ratio. SANTS is post-trained with a path-level reward computed after the frozen action branch generates the final action chunk, so the scheduler is optimized for downstream action quality rather than intermediate video fidelity, while redundant video-state updates are explicitly penalized. Experiments show that SANTS reaches \(94.4\%\) overall success on RoboTwin 2.0 and \(73.1\%\) average success across seven real-robot tasks, while reducing latency by \(81.7\%\) and \(79.0\%\) relative to full video denoising, respectively. These results indicate that adaptive selection along the video noise trajectory can preserve the control benefits of WAM-style future reasoning while removing much of its redundant inference cost.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：世界动作模型（WAM）通过预测未来视频来调节动作生成，但其推理过程高度依赖昂贵的迭代式视频去噪。以往方法默认采用固定步数或完全去噪的视频作为动作条件，忽略了视频去噪的**状态依赖性**——粗粒度运动（如自由空间移动）在浅层去噪下即可提供足够动作线索，而接触、对齐等精细操作需要更深层去噪；此外，过度去噪可能引入物理不合理的未来预测，反而降低动作质量。
- **整体含义**：提出一种轻量级调度器 SANT，在视频去噪过程中自适应选择终止点与步长，从而在保持 WAM 未来推理优势的同时大幅削减冗余推理成本，实现更好的成功率-延迟权衡。

## 2. 论文提出的方法论

### 核心思想
- 将固定视频去噪过程转变为**状态自适应噪声轨迹控制**——在每个决策点，调度器根据当前视频状态表示和噪声水平，联合预测：
  1. **累积停止风险**：决定当前中间视频表示是否已足够用于动作生成。
  2. **相对噪声进展比**：若需要继续去噪，则预测下一步应沿噪声轴前进多少比例（而非固定到下一个预定义噪声级）。
- 调度器通过**路径级强化学习奖励**进行后训练，该奖励由冻结的动作分支生成的动作块质量（与演示动作的误差）和视频更新次数共同构成，从而优化下游动作质量而非视频保真度。

### 关键技术细节
- **冻结视频-动作策略接口**：SANT 附着在预训练的视频-动作扩散策略上，仅读取当前视频特征 `zk` 和噪声水平 `σk`，不更新骨干网络、视频头、动作头或动作去噪过程。
- **停止机制**：
  - 使用**累积危险函数**：`ΔHk = softplus(g(zk, σk))`，累加得到 `Hk`，停止概率 `Fk = 1 - e^{-Hk}`。
  - 训练时使用条件停止概率 `hk = 1 - e^{-ΔHk}` 进行随机采样；部署时当 `Fk > 阈值 η` 则停止。
- **相对噪声进展**：
  - 对每一步继续决策，采样相对噪声比例 `rk ~ Beta(αk, βk)`，实际噪声更新为 `σ_{k+1} = rk σk`。
  - 训练采样随机，部署使用预测的 `mk`（Beta分布众数）作为确定性比例。
  - 更新公式：`x_{k+1} = x_k + v̂φ(x_k, σ_k, c)(σ_{k+1} - σ_k)`（一阶流匹配）。
- **路径级奖励**：
  - 动作质量误差包含**序列误差**（位置、朝向、夹爪状态）和**时间差分误差**（运动趋势匹配）。
  - 使用浅度去噪和完全去噪两条参考路径进行归一化，得到可恢复的动作质量增益。
  - 计算视频更新的成本（加权更新次数），最终奖励 `R(τ) = Q(τ) - C(nτ)`。
  - 调度器使用 PPO 进行后训练，仅更新调度器参数，冻结整个骨干。

### 伪算法流程说明
1. 初始化视频潜变量 `x0`，噪声 `σ0 = 1`。
2. 循环直到停止：
   - 冻结的 Transformer 对当前视频进行正向传播，得到特征 `zk`。
   - SANT 根据 `(zk, σk)` 预测 `ΔHk` 和 `(mk, ck)`。
   - 若累计停止概率 `Fk > η`，则终止循环，将当前 `xk` 和噪声 `σk` 传给动作分支生成动作块。
   - 否则，采样或直接使用 `mk` 计算 `σ_{k+1}`，并执行一步视频更新，增加更新计数。
3. 动作分支（冻结）根据最终视频表示生成动作块，与环境交互。

## 3. 实验设计

- **数据集与场景**：
  - **模拟**：RoboTwin 2.0（50 个任务，包含双机械臂精细操作、物体接触、随机化多任务场景）。
  - **真实机器人**：两个平台共 7 个任务——AgileX 双机械臂（衣物折叠、背包打包、袜子放置、充电器插入）、UR10 厨房平台（盘子转移、冰箱放置、水果分拣）。
- **基准方法**：
  - 模拟：LingBot-VA、Motus、Fast-WAM、π0.5。
  - 真实：π0.5、与 SANT 共享骨干的完全去噪 WAM（Full-Denoising WAM）。
- **评估指标**：任务成功率（主要）、端到端推理延迟（毫秒）。
- **训练细节**：
  - 骨干为 Wan2.2-5B 视频-动作扩散策略。
  - 骨干先微调 30000 步，然后冻结，调度器微调 3000 步（实机场景）。
  - 所有方法使用相当的总预算。

## 4. 资源与算力

- 文中未明确报告训练使用的 GPU 型号、数量、具体时长。仅在 Table 1 标注推断延迟在 A100 GPU 上测量。附录给出了调度器超参数，但未涉及训练算力细节。因此本文在算力方面信息不完整。

## 5. 实验数量与充分性

- **实验数量**：
  - 离线诊断：500 个人工标注的演示片段，扫描 7 个去噪深度。
  - 模拟：50 个任务 × 50 次试验 (共 2500 次评估) + 三随机种子验证稳定性。
  - 真实：7 个任务 × 50 次试验 (共 350 次评估)。
  - 消融：4 种变体（Fixed-5-step, Fixed-full, Stop-only, Jump-only）在模拟上比较 + 附录中奖励函数消融 5 种变体。
- **充分性**：
  - 实验覆盖了模拟和真实场景，任务类型多样（双机械臂、变形物体、精确插入、容器操作、多目标分类）。
  - 对比方法包括多种主流 WAM 和 VLA（π0.5），具有代表性。
  - 消融实验验证了停止和跳跃两个决策模块各自贡献。
  - 奖励函数消融验证了路径级奖励设计的必要性。
  - 但真实任务仅有两个平台，通用性有待扩展。总体而言实验设计较为充分、方法对比公平（相同骨干、预算、评估协议）。

## 6. 论文的主要结论与发现

- SANT 在 RoboTwin 2.0 上达到 **94.4%** 成功率（三随机种子均值），延迟 523.7 ms，比完全去噪的 WAM (LingBot-VA) 成功率更高且延迟降低 81.7%。
- 在七个真实机器人任务上平均成功率 **73.1%**，优于 π0.5 (54.6%) 和完全去噪 WAM (65.7%)，延迟降低 79.0% (至 581.3 ms)。
- **关键发现**：动作生成的最佳视频条件不是完全去噪的点，而是沿噪声轨迹的状态依赖点；自适应停止和自适应跳跃是互补的，联合建模获得最好成功率-延迟权衡。

## 7. 优点

- **问题识别新颖**：首次系统论证了像素空间 WAM 中视频去噪的**状态依赖性**，指出固定完全去噪不是最优条件。
- **方法轻量高效**：SANT 是轻量级可插拔调度器（仅增加少量参数），可附着于兼容的 WAM 骨干，无需修改骨干或手动标注。
- **动作质量驱动的训练**：使用下游动作质量的路径级奖励而非视频保真度，实现针对控制目标的优化。
- **显著的效率提升**：在保持甚至提高成功率的同时，消除了约 80% 的视频去噪推理开销。
- **实验严谨**：离线诊断、模拟、实机、消融多层次验证，且确保基准方法使用相同骨干和预算条件下比较。

## 8. 不足与局限

- **实验覆盖有限**：仅在 RoboTwin 2.0 和两个真实平台（AgileX、UR10）上评估，更多实施例、相机设置、任务族和视频-动作骨干下的泛化性尚未验证。
- **冻结骨干假设**：SANT 保持视频-动作骨干冻结，联合优化骨干与调度器可能进一步提升性能（但文中未探索）。
- **超参数依赖**：部署时依赖停止阈值 η、成本权重 λc 等超参数，在不同场景可能需要调节。
- **成本类型单一**：主要减少视频去噪成本，但未将自适应预算分配扩展到动作去噪、感知、通信或控制频率。
- **离线诊断的局限性**：深度扫描仅在固定骨干上离线进行，未在在线闭环中验证状态依赖性的实时变化。

（完）
