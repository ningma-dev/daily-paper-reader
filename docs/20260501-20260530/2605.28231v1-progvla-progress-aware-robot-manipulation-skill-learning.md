---
title: "ProgVLA: Progress-Aware Robot Manipulation Skill Learning"
title_zh: ProgVLA：进度感知的机器人操作技能学习
authors: "Seungsu Kim, Jinyoung Choi, Seungmin Baek, Jean-Michel Renders"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28231v1"
tags: ["query:robot-rl"]
score: 9.0
evidence: 使用离线强化学习微调VLA策略并引入进度表征
tldr: 现有VLA模型处理长多模态序列时面临计算和内存压力，效率低下。ProgVLA通过两阶段Perceiver重采样压缩序列长度，同时引入辅助进度头利用离线强化学习估计任务进度，实现进度感知的流匹配模仿学习。0.1B参数模型在机器人操作基准上达到与更大模型竞争的性能，并在长时和困难任务上表现更优。该工作为资源受限下的可靠机器人操作提供了紧凑实用的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28231v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28231v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 791, \"height\": 503, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28231v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1269, \"height\": 869, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1168, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1485, \"height\": 1023, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1087, \"height\": 671, \"label\": \"Table\"}]"
motivation: 现有VLA模型处理长序列时计算开销大，缺乏对任务进度的显式建模。
method: 采用两阶段Perceiver重采样压缩多模态序列，并引入离线RL进度头实现进度感知的流匹配模仿学习。
result: 0.1B参数模型在标准基准上媲美大模型，长时和困难任务上超越，消融验证组件有效性。
conclusion: 提出的紧凑进度感知VLA模型在资源受限下实现了高效可靠的机器人操作。
---

## 摘要
我们提出了ProgVLA，一个紧凑的视觉-语言-动作（VLA）模型，专为在严格的计算和内存预算下进行可靠的机器人操作而设计。该模型特别专注于通过维护任务进度的显式表示来高效处理长多模态序列。为此，ProgVLA集成了两个关键组件。首先，采用两阶段Perceiver重采样方案的多模态编码器，将可变长度的视觉、语言和本体感受流压缩为一组固定的控制就绪上下文令牌，在保持跨模态对齐的同时显著减少序列长度。其次，使用离线强化学习（RL）目标训练一组辅助进度头，以共同学习关于归一化剩余时间目标的评判器。这为策略提供了任务进度的内部估计，并实现了基于优势和成功加权的流匹配模仿学习。在两个成熟的多任务机器人操作基准测试中，一个0.1B参数的ProgVLA模型达到了与更大规模的预训练基线相竞争的成功率，并且在长时域和更困难的任务层级上超过了这些基线。消融实验表明，学习的上下文重采样器和任务自适应视觉微调是最大的单一贡献因素，而进度感知训练则提供了持续性的额外增益，主要集中在长时域和多物体任务上。我们还在真实世界的玩具厨房环境中进一步验证了该方法。

## Abstract
We present ProgVLA, a compact vision-language-action (VLA) model designed for reliable robot manipulation under tight compute and memory budgets. The model specifically focuses on efficiently processing long multi-modal sequences by maintaining an explicit representation of task progress over extended horizons. To this end, ProgVLA integrates two key components. First, a multi-modal encoder with a two-stage Perceiver resampling scheme compresses variable-length visual, language, and proprioceptive streams into a fixed set of control-ready context tokens, substantially reducing sequence length while preserving cross-modal grounding. Second, an auxiliary set of progress heads is trained with offline reinforcement learning (RL) objectives to jointly learn critics over normalized remaining-horizon targets. This provides the policy with an internal estimate of task progress and enables advantage- and success-weighted flow-matching imitation learning. On two well-established multi-task robot manipulation benchmarks, a 0.1B-parameter ProgVLA model reaches success rates that are competitive with, and on long-horizon and harder task tiers exceed, substantially larger pretrained baselines. Ablations indicate that the learned context resampler and task-adaptive visual fine-tuning are the largest single contributors, while progress-aware training provides a consistent additional gain that is concentrated on long-horizon and multi-object tasks. We further validate the approach in real-world toy-kitchen environments.

---

## 论文详细总结（自动生成）

# 论文详细分析总结

## 1. 核心问题与整体含义（研究动机与背景）

- **研究动机**：现有的大规模视觉-语言-动作（VLA）模型（如OpenVLA、π0、SmolVLA等）依赖数十亿参数和大规模跨本体机器人预训练，导致计算成本高、部署门槛高。特别是，紧凑型VLA模型在处理长时域任务时表现显著下降，缺乏对任务进度的显式建模。
- **核心问题**：如何在有限计算和内存预算下，让一个紧凑的VLA模型（<1B参数）在长时域和困难操作任务上达到甚至超越大规模预训练模型的性能？关键在于设计高效的多模态序列压缩、强视觉先验以及内部进度感知训练。
- **整体含义**：ProgVLA证明了通过精心设计的架构（两阶段Perceiver重采样、通用视觉骨干微调、辅助进度头）可以在完全不使用大规模机器人预训练的情况下，在标准多任务基准上超越十亿参数级别的预训练模型，为资源受限场景下的可靠机器人操作提供了实用路径。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：通过两阶段Perceiver重采样将多模态输入压缩成固定小尺寸的上下文令牌，并引入离线强化学习（Offline RL）风格的辅助进度头，为策略提供内部任务进度估计，从而对模仿学习损失进行加权。
- **关键技术细节**：
  1. **多模态编码器**：
     - 视觉：使用预训练的DUNE（ViT-Small蒸馏模型）提取特征，语言：使用冻结的T5文本编码器，本体感受：MLP投影。
     - 第一阶段Perceiver重采样：对每个模态单独压缩成固定数量的令牌（视觉8个，语言16个）。
     - 跨模态融合Transformer（2层Pre-LN）。
     - 第二阶段后融合Perceiver重采样：将融合后的令牌序列进一步压缩成4个控制就绪的上下文令牌。
  2. **动作专家**：采用SmolVLA风格的流匹配（Flow Matching）动作生成，预测长度为16的动作块，执行时仅执行前8步，然后重新规划。训练时使用Beta(2,2)采样噪声插值时间τ。
  3. **辅助进度头**：
     - 定义归一化进度目标 \( r_t = \max(0, 1 - (T - t)/T_M) \)，其中 \( T_M = 500 \)（固定地平线上限）。
     - **状态-动作价值头 (Q)**：以上下文令牌和第一个动作作为输入，用Huber损失回归 \( r_t \)。
     - **状态价值头 (V)**：仅以上下文令牌输入，使用IQL风格的期望分位数回归（ρ=0.8），产生乐观基线。
     - **成功分类头 (S)**：二元交叉熵，标签为 \( r_t \geq r_{succ} \)（约最后17步）。
     - 优势 \( A_t = Q(c_t, a_t) - V(c_t) \) 被切断梯度后转化为权重 \( w_{A,t} = \min(\exp(A_t / \beta), C) \)，\( w_{S,t} = 0.5 + 0.5\sigma(S(c_t)) \)。
  4. **总损失**：流匹配损失乘以 \( w_A \cdot w_S \)，再加上 \( \lambda_V L_V + \lambda_Q L_Q + \lambda_S L_S \)，权重分别为0.5, 1.0, 0.2。
- **公式/算法流程**（文字说明）：
  - 训练时：每步采样观测和指令→编码得到上下文令牌→对每个样本计算进度目标→更新Q、V、S头→计算优势并转权重→加权流匹配损失→反向传播。
  - 推理时：仅使用动作专家（流匹配ODE积分10步Heun），丢弃进度头。

## 3. 实验设计：数据集、基准与对比方法

- **模拟基准**：
  - **LIBERO**：40个任务（分为Spatial, Object, Goal, Long四个类别），每个任务50条示范，共2000条轨迹。官方评估协议（每任务50轮）。
  - **Meta-World MT50**：使用LeRobot公开数据集，实际49个任务（50个中缺1个），分Easy/Medium/Hard/Very Hard四个难度层级，每任务50条示范。评估协议同SmolVLA。
- **真实世界实验**：6-DOF PiPER机械臂，腕部+外部相机，共10个任务（8个玩具厨房+2个白色桌面），每个任务50条示范，评估100次（10次/任务）。
- **对比方法**：
  - 大规模模型：OpenVLA (7B)、π0 (3.3B和Paligemma-3B)、Octo (0.09B)。
  - 紧凑模型：SmolVLA (0.24B和2.25B)。
  - 基线分数来自原论文（未重跑，但有协议差异说明）。
- **消融实验**（在LIBERO上）：
  1. 移除进度目标（无辅助头）。
  2. 移除后融合上下文重采样器。
  3. 用DINOv3替换DUNE骨干。
  4. 冻结DUNE（不微调）。

## 4. 资源与算力

- **硬件**：单张NVIDIA H100 GPU。
- **训练时间**：LIBERO模型25小时，Meta-World模型32小时，真实世界模型8.5小时。
- **参数规模**：完整模型109M参数（双相机），训练时更新74M（仅DUNE和T5预训练，其余随机初始化）。Meta-World版本100M参数（单相机）。
- **代码与数据集**：声明将开源。

## 5. 实验数量与充分性

- **实验数量**：三大类实验（LIBERO、Meta-World、真实世界）加上LIBERO上的4组消融实验。每个基准覆盖数十个任务，每任务50轮评估，统计充分。
- **充分性与公平性**：
  - **优点**：消融实验设计清晰，覆盖架构、骨干、进度监督三个关键维度；真实世界验证了迁移性；提供了失败案例分类（32个失败案例的详细分析）。
  - **不足**：
    - 基线对比非严格等条件下重跑（协议差异：数据量、图像分辨率、评估轮次不同），作者承认这一局限。
    - 仅单种子训练，未报告多次运行的统计方差（仅提及约±1~2个百分点的波动）。
    - 未进行超参数敏感度分析（如 \( T_M \), ρ, β, C）。
    - 进度头的直接验证不足（如校准曲线、单调性分析）。
    - 真实世界评估仅在两个固定环境内，未测试泛化到新物体/新场景。

## 6. 主要结论与发现

- **性能对比**：
  - LIBERO：ProgVLA平均91.1%，比SmolVLA 2.25B高2.4%，比OpenVLA 7B高14.6%。长时域任务上88.6% vs SmolVLA 77%（+11.6%）。
  - Meta-World：ProgVLA平均78.5%，比SmolVLA 2.25B高10.3%。在Very Hard层级上79.6% vs 64%（+15.6%）。
- **消融关键发现**：
  - 后融合上下文重采样器贡献最大（移除后长时域骤降至51.2%，平均降16%）。
  - 微调视觉骨干为第二大杠杆（冻结DUNE降13.5%）。
  - 进度头提供一致但较小的增益（平均+2.3%），集中在Object和Long类别。
  - DUNE优于DINOv3（尤其长时域+7.6%）。
- **真实世界**：平均成功率68%，常见失败模式为场景遮挡（10/32）、夹爪超时（8/32）、抓错物体（4/32）。
- **核心发现**：在紧凑参数下，高效的序列压缩（两阶段重采样）和强视觉先验（DUNE微调）可以替代跨本体机器人预训练；内部进度监督进一步助力长时域任务。

## 7. 优点（方法与实验亮点）

- **架构创新**：
  - 两阶段Perceiver重采样设计巧妙：先模态内归一化，后融合压缩，避免了模态不平衡问题，并且使进度头只处理少量上下文令牌，计算开销小。
  - 将离线RL思想的辅助头与流匹配模仿学习结合，通过自适应的样本权重将梯度集中于关键决策阶段（如任务末期），原理清晰且实现轻量。
  - 完全不依赖大规模机器人预训练，仅使用通用视觉+冻结T5，降低了数据门槛。
- **实验严谨性**：
  - 消融实验逐一对核心组件进行控制变量，结果解释清晰。
  - 真实世界评估不仅给出成功率，还分类分析了32个失败案例，并讨论了固有限制（干扰、超时、错误抓取等）。
  - 提供了与基线对比的协议差异详细说明（附录B.7），帮助读者判断可比性。

## 8. 不足与局限

- **实验覆盖不足**：
  - 无多粒度性能度量（如成功率外缺少其他指标：执行时间、精确度）。
  - 未进行多种子重复实验以获得统计显著性和方差报告。
  - 基线对比非严格复现，可能有数据集/分辨率差异影响。
- **进度头设计局限**：
  - 进度目标纯基于时间位置（\( r_t \)），不依赖语义或子目标，可能将不同难度的等长阶段等同对待。
  - 未验证进度头的校准性、单调性等内在质量。
  - 优势信号在仅有成功示范下弱识别，更多是轨迹阶段重加权而非严格Q信号。
- **泛化能力有限**：
  - 真实世界仅一个机械臂、两个固定环境，未测试跨平台和跨场景泛化。
  - 所有训练数据均为成功示范，未利用失败数据。
- **缺少效率基准**：未提供推理延迟、控制频率、峰值内存等系统级数据。
- **安全与伦理**：虽然讨论了安全考虑，但未提供具体安全机制验证（如硬性互锁、人类监督等）。

（完）
